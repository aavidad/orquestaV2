# orquesta-work-profiles

Estado: placeholder historico no ejecutable.

Este directorio no es owner de perfiles de trabajo. El contrato vigente vive en
`modulos/orquesta-core-workflow` como `WorkProfileV0` y
`WorkflowTaskV0.work_profile_kind`; la resolucion operativa vive en
`modulos/orquesta-orchestration-core`.

Reglas:

- No crear codigo, backlog local ni `docs/tareas.md` aqui sin decision nueva del
  director.
- No mover perfiles desde `orquesta-core-workflow` a este directorio.
- Si un scanner detecta este path, debe tratarlo como placeholder no
  programable y usar la fuente vigente anterior.
