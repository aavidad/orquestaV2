# Resultado A/B BUG-241 a BUG-243

Fecha: 2026-07-11
Alcance: local aislado, Orquesta real con Codex app-server, sin OPES ni remoto.

## Identidad

- baseline y binario: `55e61ef577344dd4f550458d7e6cee9219d934bd`,
  `vcs.modified=false`;
- SHA-256 binario: `192afad61cd5314acd213e0df8b5b60a5086de2232ac1e023dc976c9ab88acce`;
- run: `request-ref-bug242-env-varargs-ab-20260711`;
- goal: `goal-ref-task-autoprogramming-ec94eda16810-g01`;
- external goal: `019f50c2-85ec-7293-a144-affbf04b7620`;
- rework improcedente: `goal-ref-task-autoprogramming-ec94eda16810-g01-rework-1`,
  external `019f50c5-4593-70c3-8042-20a29eac3363`.

## Resultado material

Orquesta genero un diff real de 83 lineas añadidas y 15 retiradas en cinco
ficheros del write-set. El primer checkpoint de clase `diff` se observo a
53.463 tokens ajustados. El estado final previo al shutdown registro 98.256
tokens, `warning` y 44.793 desde el ultimo material. El stream del proveedor
publico 868.185 tokens acumulados, incluyendo contexto/cache; esa cifra no se
interpreta como coste facturado sin desglose del proveedor.

El parche quedo en `f91fa5ad32abfa69426ed2f9bcd5ef014a17c90c` y fue
integrado en la rama canonica como `8a43e5fd7`. El focal independiente, con
caches aisladas y snapshot de modulos sin red, paso:

```sh
go test -count=1 ./cmd/orquesta-server \
  -run 'Test(ServerEnvRegistryASTV0|CodexWave|CodexDirectorWave)'
```

## Desviacion de cierre

El request congelo el argumento de `-run` sin comillas. El ejecutor hermetico
rechazo `|` como sintaxis shell y persistio receipt `failed` con
`required_test_command_shell_syntax_prohibited`. El binder no habia aplicado
esa misma politica antes del launch, de modo que el fallo externo se redujo a
test fallido y activo un rework de implementacion. Esto es BUG-243, no fallo
del diff BUG-242.

El arreglo `808224609` valida sintaxis, allowlist y shell en el binder, repite
la guarda en la entrada legacy y deja que el lifecycle clasifique cualquier
elusion como infraestructura sin `NeedsRework`. Paquetes verificados:

- `orquesta-runtime-required-test`;
- `orquesta-goal`;
- `orquesta-app-codex-stack`.

## Control y cierre

`runs/control stop forced=true` confirmo el goal original ya completo, pero el
marcador durable siguio apuntando al rework activo. Se clasifica como evidencia
adicional de BUG-165/208C. El shutdown gobernado con
`cleanup_goal_backends=true` devolvio `shutdown_ready=true`, retiro tmux,
socket y proceso app-server, y el servidor salio con codigo cero.

Hashes de los recibos efimeros antes de limpiar `/tmp`:

- prepare: `d34aa3d61a6e3592f0a4457ea6bd19f6bf542062b321a90c2a13a16a04b9c1bd`;
- observe terminal: `8ab8cfab8a3fcc5d88b3fdac1cc09274bf1ab9cf393155bf43f567d95de0e825`;
- control: `83c23cc748fb67f33aa0cfb3173322c8f88ee6cd1af3b67c57503966a8e9fb75`;
- shutdown: `115c2c91e144c17de5bb0b55360548093c924f667352cb00de74ae6408a54a32`.

## Veredicto

- BUG-242: cerrado en codigo y focal independiente.
- BUG-243: cerrado localmente; falta replay real que demuestre rechazo antes de
  lanzar Codex y cero rework.
- BUG-241: mejora funcional demostrada, pero ahorro de tokens inconcluso; no
  subir umbrales ni declarar victoria economica con este unico tratamiento.
