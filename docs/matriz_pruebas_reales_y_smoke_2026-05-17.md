# Matriz de pruebas reales y smoke

Fecha: 2026-05-17.

## Alcance

Esta matriz convierte los pendientes de pruebas reales en casos ejecutables o
documentados. Actualizacion 2026-05-22: se ejecuto un smoke Codex real acotado
de `CODEX-REQTEST-REAL-E2E` y un smoke no-OPES temporal
`EXT-NO-OPES` con app HTTP/file, submitter real opt-in, `codex-fake`, review,
`RequiredTestRunner` y cierre operativo. OPES real no se toca salvo casos opt-in
marcados.
Actualizacion 2026-05-23: `CODEX-WAVE-REAL` y `CODEX-RECURSION-REAL` quedaron
cerrados con proveedor real opt-in; no son backlog abierto salvo regresion
demostrada.
Actualizacion 2026-05-28: el ciclo ResiGRX/autonomia cerro como regresion real
de app generada: run `run-spec-resigrx-req-resigrx-887f4566d73cdaa5c23e03d06a14da8a`
con 21/21 tareas cerradas, 21 entregas, 1 validacion, 1 closure y 4 replans.
La correccion queda cubierta por `go test -count=1 ./...`, por pruebas focales
de cierre/replan y por el servidor residente reiniciado en modo limpio. No
reabre `WaitAgentRefs`, OPES, `CODEX-WAVE-REAL` ni `CODEX-RECURSION-REAL`.

Reglas operativas:

- ejecutar pruebas reales solo sobre instancias temporales o de smoke;
- no tocar una instancia OPES que este creando un temario real;
- para OPES, exigir filtro por tipo de job cuando el bridge lo soporte
  (`ORQUESTA_OPES_BRIDGE_JOB_TYPE`) o por job exacto
  (`ORQUESTA_OPES_BRIDGE_JOB_REF`) y limites bajos
  (`ORQUESTA_OPES_BRIDGE_LIMIT`);
- no ejecutar drenados amplios de OPES para "ver que pasa";
- si `ORQUESTA_OPES_BASE_URL` no es loopback, ademas de
  `ORQUESTA_OPES_TEMPORAL_CONFIRM=1` se exige
  `ORQUESTA_OPES_BRIDGE_DESTINATION_EVIDENCE_REF` con una ref compacta que
  demuestre instancia temporal/preproduccion aislada;
- usar `ORQUESTA_*_CONFIRM=1` solo cuando el operador haya validado instancia,
  cuota, workdirs y estado aislado;
- conservar evidencias bajo `/tmp` o bajo el directorio de run indicado por el
  smoke;
- si no existe script claro, este documento marca el caso como pendiente y no
  inventa un comando nuevo.
- los smokes Go opt-in y harnesses que ejecutan procesos reales deben fallar
  con diagnosticos redactados/acotados: refs, nombres de control file, codigos y
  contadores por defecto; prompts, transcripts, completions, cuerpos HTTP,
  respuestas OPES, stdout/stderr completos, rutas privadas, HOME, env, tokens,
  remotos Git y control files crudos solo pueden conservarse en directorio
  temporal opt-in con razon local y no cuentan como evidencia terminal.
- desde el cierre T158 del 2026-05-26, `opes-drain-once` debe aplicar politica
  de destino OPES antes de drenar: URLs sin credenciales ni query, destino
  loopback o temporal confirmado, productivo bloqueado por la composicion y
  summary publico solo con refs opacas, categorias, filtros, estado y contadores.

Fuentes revisadas:

- `docs/handoff_sesion_2026-05-12_core_smoke_review_rework.md`;
- `docs/decision_director_objetivo_actual_2026-05-12.md`;
- `docs/incidente_autoprogramacion_real_2026-05-12.md`;
- `docs/plan_autoprogramacion_orquesta_2026-05-12.md`;
- `docs/estado_integracion_opes_orquesta_2026-05-13.md`;
- `docs/corte_integracion_opes_orquesta_2026-05-13.md`;
- `docs/integracion_opes_orquesta_2026-05-13.md`;
- `docs/handoff_sesion_2026-05-13_nucleo_stats_stop.md`;
- `docs/resultado_prueba_opes_orquesta_plan_2026-05-14.md`;
- `docs/resultado_prueba_opes_orquesta_plan_limpia_2026-05-15.md`;
- `docs/corte_opes_como_consumidor_orquesta_2026-05-18.md`;
- `docs/runbooks/smoke_opes_plan_temario_operadores_2026-05-18.md`;
- `docs/runbooks/smoke_opes_domain_work_real_2026-05-13.md`;
- `docs/runbooks/smoke_opes_external_work_agent_real_2026-05-13.md`;
- `docs/runbooks/smoke_opes_visual_asset_real_2026-05-13.md`;
- `docs/runbooks/resultado_smoke_opes_derivados_goal_first_real_2026-06-28.md`;
- `docs/runbooks/smoke_servidor_reinicio_estado.md`;
- `docs/runbooks/smoke_servidor_rest_director.md`;
- `docs/runbooks/self_observability_real_test_2026-05-13.md`;
- `docs/runbooks/apagado_controlado_servidor_y_agentes.md`;
- `modulos/orquesta-app-codex-stack/docs/contratos.md`;
- `modulos/orquesta-app-codex-stack/docs/pruebas.md`;
- `modulos/orquesta-app-codex-stack/docs/decisiones.md`;
- `modulos/orquesta-app-codex-stack/docs/review_rework_replan.md`;
- `modulos/orquesta-server-shutdown/docs/contratos.md`;
- `modulos/orquesta-server-shutdown/docs/pruebas.md`.

Esta matriz es la fuente de autoridad para estado de smokes y pruebas reales.
El backlog ejecutable debe citar estas filas y no relanzar casos cerrados sin
regresion.

## Matriz

Nota T158 2026-05-26: los smokes OPES que usan `opes-drain-once` conservan OPES
temporal explicito; el opt-in productivo queda bloqueado en esta composicion.
La salida publica del bridge queda en refs/categorias de destino, filtro
aplicado y contadores, sin URL completa, query sensible, host privado, payload
OPES, tokens ni cuerpos HTTP.
Revalidacion OrquestaV2 2026-06-11:
`agent-ref-task-autoprogramming-7c02f2568e45-g03` mantiene la politica T158
como guard vigente para OPES: loopback no equivale a instancia temporal
confirmada, todo efecto real requiere confirmacion temporal explicita,
`ORQUESTA_OPES_BRIDGE_PRODUCTIVE_CONFIRM=1` se rechaza como
`opes_destination_productive_not_allowed`, y
`ORQUESTA_OPES_BRIDGE_JOB_TYPE_SEQUENCE` solo es aceptable con scope duro
(`program_id`, `topic_id`, `correlation_id` o equivalente) y limite bajo. No se
ejecuta smoke real ni se abre owner de `cmd/orquesta-server` fuera del
write-set; el contexto `ref_only` requerido queda resuelto por lectura local y
evidencia explicita en ACK.
Rework OrquestaV2 2026-06-11:
`agent-ref-task-ref-review-rework-task-autoprogramming-7c02f2568e45-g01-449fcc36b3c508d55429e771db52f5fb`
revalida `SERVER-SELF-WATCHDOG-OFFLINE` tras el checkpoint de revision: pasan
`git diff --check`, la bateria amplia requerida y los focales
`go test -count=1 ./cmd/orquesta-server` y
`go test -count=1 ./modulos/orquesta-app-codex-stack`. No ejecuta smokes OPES ni
abre efectos externos.
Revalidacion OrquestaV2 2026-05-26:
`agent-ref-task-autoprogramming-bf0d81417acc-g01` confirma que esta politica
sigue vigente sin reabrir owners vecinos; el contexto `ref_only` requerido queda
resuelto por lectura local y evidencia explicita en ACK.
Retry OrquestaV2 2026-05-26:
`agent-ref-task-autoprogramming-761a129dc1b4-g01` revalida este cierre sin
abrir smoke real ni owner vecino; el contexto `ref_only` requerido queda
resuelto por lectura local y evidencia explicita en ACK.

| ID | Caso | Tipo | Prerrequisitos y env vars | Comando existente | Criterio de exito | Riesgos y limites |
| --- | --- | --- | --- | --- | --- | --- |
| OPES-SECUENCIA-CANON-2026-06-07 | Nota de autoridad para filas OPES historicas. | Documental. | Aplica a cualquier smoke OPES de temario o derivados. | Canon operativo: `modulos/orquesta-opes-bridge/temario_sequence_v0.go`, `docs/opes_flujo_temario_operativo_2026-06-02.md` y `docs/runbooks/handoff_opes_derivados_reales_hasta_local_html_site_2026-06-07.md`. | La cadena principal vigente es `plan_temario -> update_topic_registry -> research_exam_precedents -> draft_content_block -> generate_visual_asset -> generate_question_bank -> review_legal -> review_pedagogical -> review_quality -> review_codex -> review_gemini -> review_claude -> review_pair_codex_gemini -> review_pair_codex_claude -> review_pair_gemini_claude -> review_director_consolidation -> validate_topic -> assemble_topic -> generate_audio_asset -> generate_tutor_assets -> generate_learning_games -> visual_asset_reuse -> generate_html_site -> generate_help_manual_assets -> finalize_temario_package`. `generate_agent_candidate_*`, `vote_agent_candidates_*` y `select_agent_candidate_director` son rework opcional con refs causales, no pasos fijos. | Las filas historicas que muestren secuencias parciales, audio despues de HTML o candidatos/votos fijos quedan subordinadas a esta nota hasta que se limpien una a una. |
| OPES-PLAN-REAL | OPES `plan_tema` completo: OPES crea job, Orquesta drena, Codex real entrega `document_plan`, OPES completa y crea derivados. | Historico real, invasivo, con cuota; no reejecutar salvo legacy explicito. | OPES temporal con job `plan_tema` pendiente; Orquesta temporal levantada con `ORQUESTA_OPES_BASE_URL` al mismo OPES; `ORQUESTA_BASE_URL`; Codex configurado por operador; `ORQUESTA_OPES_BRIDGE_CONFIRM=1`; `ORQUESTA_OPES_TEMPORAL_CONFIRM=1`; `ORQUESTA_OPES_BRIDGE_JOB_TYPE=plan_tema`; `ORQUESTA_OPES_BRIDGE_LIMIT=1`. | Historico: `go run ./cmd/orquesta-server opes-drain-once` con las env anteriores. Para ejecucion vigente usar `OPES-DER-RESTO` goal-first con `app_server_tmux`. | Un solo artefacto `document_plan` en OPES; job `plan_tema` `completed`; sin duplicar `document_plan`; jobs derivados creados desde el plan con tipos ejecutables (`research_exam_precedents`, `draft_content_block`, `generate_visual_asset`, `generate_question_bank`, `review_*`, `validate_topic`, `assemble_topic`, `generate_audio_asset`, `generate_tutor_assets`, `generate_html_site`). La entrega de `assemble_topic` debe validarse como `assembled_topic`, la fase auditiva como `audio_asset`, tests como `question_bank`, tutor como `tutor_bot_package` y HTML local como `local_html_site`. | No ejecutar contra OPES activo. Si se omite `ORQUESTA_OPES_BRIDGE_JOB_TYPE=plan_tema`, el bridge puede enviar derivados y lanzar trabajo no focal. Puede consumir decenas de miles de tokens. |
| OPES-PLAN-TEMARIO-OPERADORES | OPES `plan_temario` de Operario como `document_plan`: OPES temporal, Orquesta bridge REST residente, supervisor, Codex real xhigh, entrega y derivados. | Real acotado cerrado para plan; automatizacion de derivados por fases cubierta offline. | OPES temporal con `Operario.txt`; Orquesta temporal con `ORQUESTA_OPES_BASE_URL`; `ORQUESTA_OPES_BRIDGE_CONFIRM=1`; `ORQUESTA_OPES_TEMPORAL_CONFIRM=1`; para plan usar `ORQUESTA_OPES_BRIDGE_JOB_TYPE=plan_temario` y `ORQUESTA_OPES_BRIDGE_JOB_REF=<job>`; para derivados usar la secuencia canonica desde `update_topic_registry` hasta `finalize_temario_package`; `ORQUESTA_OPES_BRIDGE_LIMIT=1`; Codex `gpt-5.5` con `ORQUESTA_CODEX_REASONING_EFFORT=xhigh`. | Runbook `docs/runbooks/smoke_opes_plan_temario_operadores_2026-05-18.md`; evidencia `docs/resultado_prueba_opes_orquesta_plan_temario_operario_2026-05-18.md`. Tests focales: `go test -count=1 ./modulos/orquesta-opes-bridge`; `go test -count=1 ./cmd/orquesta-server -run 'TestOPESBridgeLoop|TestRunOPESDrainOnceV0'`. | Cerrado el 2026-05-18 para plan: `job_ref=9784a562f074769a08043707fdd79eb2`, `artifact_id=576d0a93563c2c40e7a6dc41c74261e8`, job OPES `completed`, 20 derivados creados sin audio porque la fase no existia entonces. Aniadido despues: el bridge automatiza pases, no avanza si el primer tipo pendiente ya fue enviado segun ledger y la prueba offline debe recorrer derivados hasta `generate_html_site`, con `topic_registry_update`, `exam_research_report`, `question_bank`, `audio_asset`, `tutor_bot_package`, `learning_games_package` y `local_html_site`. | Historico: el smoke real completo de derivados quedo cerrado despues en `OPES-DER-RESTO` por goal-first hasta `finalize_temario_package`; esta fila conserva la evidencia del plan de 2026-05-18. OPES no filtra por `program_id`; ejecutar secuencia contra OPES temporal o cola acotada. MCP/MCPO no participa; REST es la via funcional. |
| OPES-DOMAIN-REST | Puente REST directo `domain_work` para `draft_content_block -> content_block`, idempotencia incluida. | Real temporal/fake aislado, sin agente Codex. | OPES y Orquesta temporales sanos; Orquesta con `ORQUESTA_OPES_BASE_URL`; `ORQUESTA_OPES_DOMAIN_SMOKE_CONFIRM=1`; `ORQUESTA_OPES_TEMPORAL_CONFIRM=1`; `ORQUESTA_BASE_URL`; `OPES_BASE_URL`; `SMOKE_ID`; opcional `SMOKE_OUT_DIR`; `ORQUESTA_OPES_BRIDGE_PRODUCTIVE_CONFIRM` prohibido. | `scripts/smoke_opes_domain_work_real.sh`. | Crea topic/chapter de smoke en OPES temporal; crea job OPES desde Orquesta; entrega `content_block`; replay no duplica bloque; `summary.txt` contiene `job_ref`, `receipt_ref` y `block_count_after_replay=1`. | Crea datos solo en OPES temporal. No ejecutar contra OPES productivo. No valida agente real ni calidad editorial. |
| OPES-DER-DRAFT-AGENT | Derivado OPES `draft_content_block` con agente real por flujo vigente: `/api/v0/external-work/run -> goal_first con ORQUESTA_CODEX_GOAL_BACKEND=app_server_tmux -> external_goal_ref -> goal/observe -> submit_artifact`; `legacy_director_loop` queda solo como replay historico opt-in y no cierra evidencia vigente. | Real temporal, invasivo, con cuota; subordinado a `OPES-DER-RESTO` para derivados/cierre OPES y a `CODEX-GOAL-FIRST-APP-SERVER-REAL` para la validez del backend app-server. | OPES temporal confirmado; `ORQUESTA_OPES_AGENT_SMOKE_CONFIRM=1`; `ORQUESTA_OPES_TEMPORAL_CONFIRM=1`; `ORQUESTA_CODEX_GOAL_BACKEND=app_server_tmux`; `OPES_BASE_URL`/`ORQUESTA_OPES_BASE_URL`; `ORQUESTA_BASE_URL` si usa servidor residente; scope duro por `JOB_REF`, `correlation_id`, `topic_id` o cola temporal dedicada; `ORQUESTA_CODEX_MODEL`; `ORQUESTA_CODEX_REASONING_EFFORT`; timeout/poll acotados; `ORQUESTA_OPES_BRIDGE_PRODUCTIVE_CONFIRM` prohibido. | `scripts/smoke_opes_external_work_agent_real.sh`; para cierre funcional amplio usar `OPES-DER-RESTO` y el runbook goal-first 2026-06-28. | La creacion de run devuelve `director_execution_mode=goal_first` y `external_goal_ref`; la observacion del Goal termina con `goal_status=complete`, `closure_status=accepted` y `closure_accepted=true`; OPES temporal recibe `content_block`/artefacto con receipt causal; no hay caida al loop legacy; Orquesta apaga servidor temporal de forma controlada. | Consume cuota y crea datos solo en OPES temporal. No probar contra productivo ni sin scope duro. No prueba ensamblado de tema completo; el cierre operativo de derivados queda en `OPES-DER-RESTO`. |
| OPES-DER-VISUAL-REST | Derivado OPES `generate_visual_asset -> visual_asset` por REST directo. | Real temporal/fake aislado, sin agente Codex. | OPES temporal actualizado que acepte `generate_visual_asset`; Orquesta temporal con `ORQUESTA_OPES_BASE_URL`; `ORQUESTA_OPES_VISUAL_SMOKE_CONFIRM=1`; `ORQUESTA_OPES_TEMPORAL_CONFIRM=1`; `ORQUESTA_BASE_URL`; `OPES_BASE_URL`; `SMOKE_ID`; `ORQUESTA_OPES_BRIDGE_PRODUCTIVE_CONFIRM` prohibido. | `scripts/smoke_opes_visual_asset_real.sh`. | Un job `generate_visual_asset`; un artefacto `visual_asset`; un bloque OPES temporal `visual_asset`; `format=svg`, `content_type=image/svg+xml`, `caption` y `alt_text` presentes. | No valida agente real para visual. Instancias OPES antiguas pueden responder `invalid document job`. No ejecutar contra OPES productivo. |
| OPES-DER-RESTO | Derivados de plan: `update_topic_registry`, `research_exam_precedents`, `draft_content_block`, `generate_visual_asset`, `generate_question_bank`, `review_legal`, `review_pedagogical`, `review_quality`, `validate_topic`, `assemble_topic`, `generate_audio_asset`, `generate_tutor_assets`, `generate_learning_games`, `visual_asset_reuse`, `generate_html_site`. | Real temporal goal-first cerrado el 2026-06-28 hasta `completed_syllabus_package`; automatizacion de pases implementada y cubierta offline; wrapper operador opt-in mantenido para dry-run/drain acotado; pendiente residual de optimizaciones posteriores de contexto/coste. | OPES temporal con jobs derivados concretos; Orquesta temporal goal-first para modo con efectos; `ORQUESTA_OPES_DERIVATIVES_SMOKE_CONFIRM=1`; `ORQUESTA_OPES_TEMPORAL_CONFIRM=1`; para crear runs tambien `ORQUESTA_OPES_DERIVATIVES_EXECUTE=1`, `ORQUESTA_BASE_URL` explicito, `ORQUESTA_OPES_BRIDGE_LIMIT=1`, scope operativo por `correlation_id`, `topic_id`, cola temporal dedicada o `program_id` con `ORQUESTA_OPES_BRIDGE_SCOPE_FILTER_CONFIRMED=1` tras `scope-probe`, y `ORQUESTA_OPES_BRIDGE_SPEECH_SYNTHESIS_CAPABILITY=available` si la secuencia puede llegar a `generate_audio_asset`; `ORQUESTA_CODEX_GOAL_BACKEND=app_server_tmux` o confirmacion externa explicita de Orquesta ya arrancada en goal-first app_server_tmux con `ORQUESTA_OPES_DERIVATIVES_ORQUESTA_GOAL_FIRST_CONFIRMED=1`; politica de modelo `gpt-5.5`/`xhigh` o superior si aplica. | Probe sin efectos de filtro OPES: `ORQUESTA_OPES_DERIVATIVES_SMOKE_MODE=scope-probe scripts/smoke_opes_derivatives_rest.sh`. Preflight sin OPES/Codex: `ORQUESTA_OPES_DERIVATIVES_SMOKE_MODE=preflight-only scripts/smoke_opes_derivatives_rest.sh`. Dry-run acotado: `scripts/smoke_opes_derivatives_real.sh`. Crear una tanda: `ORQUESTA_OPES_DERIVATIVES_SMOKE_MODE=drain-once scripts/smoke_opes_derivatives_real.sh`. Servidor/bridge residente goal-first: `orquesta-server start` con `ORQUESTA_CODEX_GOAL_BACKEND=app_server_tmux`, `ORQUESTA_RUNTIME_DIR/base_url.txt`, `ORQUESTA_SERVER_URL` para el wrapper y parada por `orquesta-server stop`; la secuencia sigue documentada en el runbook. Tests offline: `bash -n scripts/smoke_opes_derivatives_real.sh`; `go test -count=1 ./modulos/orquesta-opes-bridge`; `go test -count=1 ./cmd/orquesta-server -run 'TestOPESBridgeLoop|TestRunOPESDrainOnceV0|TestSmokeOPESDerivativesRESTWrapperPreflight|TestSmokeOPESDerivativesRESTWrapperScopeProbe'`. | Offline: mapeo de tipos derivados a artefactos esperados, secuencia por fases, ledger/idempotencia, `update_topic_registry -> topic_registry_update`, `research_exam_precedents -> exam_research_report`, `assemble_topic -> assembled_topic`, `generate_audio_asset -> audio_asset`, `generate_question_bank -> question_bank`, `generate_tutor_assets -> tutor_bot_package`, `generate_learning_games -> learning_games_package`, `visual_asset_reuse -> visual_reuse_manifest` y `generate_html_site -> local_html_site`. Wrapper: rechaza ejecucion sin OPES temporal confirmado, rechaza productivo/unfiltered, rechaza `program_id` sin filtro real confirmado o cola/scope alternativo, `scope-probe` consulta OPES temporal con filtro positivo y negativo antes de declarar filtro confirmado, exige goal-first confirmado, exige `speech_synthesis` declarada cuando la secuencia puede llegar a audio, rechaza mezcla de `JOB_TYPE`/`JOB_REF` con secuencia y deja salidas en `/tmp/opes-salidas/derivatives-*`. Evidencia real 2026-06-28: el runbook goal-first `final3` demuestra OPES temporal `127.0.0.1:18196`, Orquesta temporal `127.0.0.1:19196`, loop legacy desactivado, 24/24 jobs completados con la secuencia vigente entonces, `pending=[]`, artefactos/receipts por contrato, audio/tutor/juegos/HTML/manual y `finalize_temario_package -> completed_syllabus_package`; actualizacion 2026-06-30 anade `visual_asset_reuse -> visual_reuse_manifest` con cobertura offline y queda pendiente solo si se exige re-smoke temporal largo de la secuencia actualizada. | Riesgo de mezclar derivados reales con smoke si OPES no esta acotado. Si OPES no filtra realmente por `program_id`, usar `correlation_id`, `topic_id` o cola temporal dedicada. Riesgo editorial: artefactos formalmente validos pero pobres o con placeholders. La generacion audio/TTS vive en adaptador/capability OPES bridge, no en el nucleo de Orquesta. El cierre operativo OPES real ya quedo cerrado en instancia temporal por goal-first; cualquier reejecucion debe seguir exigiendo instancia temporal, cuota/modelo y refs causales de aceptacion por derivado. |
| EXT-NO-OPES | App externa no-OPES por contrato generico `external_work`/`DomainWork`, required tests de dominio por refs/criterios/puerto y cierre por refs opacas. | Offline determinista para cierre neutral; contrato de required tests de dominio cerrado en `orquesta-domain-work`; smoke servidor temporal con app externa HTTP/file no-OPES, submitter real opt-in, entrega con `codex-fake`, review viva, `RequiredTestRunner` local, evidencia durable y cierre operativo. | Sin OPES; usar tests con fake executor/source; dominio externo debe entrar por refs compactas, acceptance criteria y `DomainWorkRequiredTestPolicyPortV0`, no DB/rutas internas ni banco comun. Para smoke real no-OPES: `go`, `curl`, `jq`, `python3`; opcional `ORQUESTA_KEEP_SMOKE_DIR=1`, `ORQUESTA_SMOKE_ROOT`, `ORQUESTA_SMOKE_REQUEST_TIMEOUT_SECONDS`, `SMOKE_ID`. | `go test -count=1 ./modulos/orquesta-domain-work ./modulos/orquesta-domain-work-http ./cmd/orquesta-server ./modulos/orquesta-app-change ./modulos/orquesta-app-codex-stack ./modulos/orquesta-app-director-service -run 'TestDomainWork(JobRequestV0TransportaTestsRequeridosDeDominio|JobRequestV0RechazaRequiredTestRefNoCompacta|RequiredTestPolicyV0DevuelvePlanPorPuertoDeDominio)|TestClientV0|TestDomainWorkExecutorFromEnvV0ConectaHTTPNeutralOptIn|TestBuildStackFromEnvV0CableaHTTPNeutralParaDomainWorkYDeliveryV0|TestDomainWorkJobRequestFromAppChangeV0ConvierteExternalWork|TestCodexStackV0ExternalWorkRunAceptaContratoAmplioV0|TestCodexStackV0ExternalWorkRunSupervisorConsumeDeliverySinExpirarWaitV0|TestCodexStackRunSupervisorAPIV0BloqueoRequiredTestsEvidenceMissingDevuelveOKV0|TestContinueRequestWithOperationalDirectorPlanStateV0RecuperaDeliveryTardiaTrasWaitExpirado|TestMaybeCloseOperationalDirectorV0(CierraComposicionExternaNeutralPorRefsOpacas|NoCierraComposicionExternaFakeConDetallesInternos)|TestAppDirectorServiceOperationalClosureExternalFakeV0NoImportaProductAdapters'`; `./scripts/smoke_external_domain_fake_real.sh`; `./scripts/smoke_external_domain_non_opes_real.sh`; runbook `docs/runbooks/smoke_external_domain_non_opes_real_2026-05-22.md`. | `AppChange` convierte `external_work` de `project-ref-agenda` en `DomainWorkJobRequestV0`; refs deduplicadas y compactas; `DomainWorkJobRequestV0.required_tests` transporta `test_ref`, criteria refs, input refs, external refs y evidencias; `DomainWorkRequiredTestPolicyPortV0` deja la resolucion productiva en el adaptador de dominio sin strings OPES ni banco comun en core; `/api/v0/external-work/run` acepta contrato amplio sin director inicial; `app-director-service` cierra una composicion externa fake con `OperationalClosureSource` por refs opacas de task, entrega, accepted review, entidad y artefacto, sin Codex, OPES ni tests de programacion inventados; el source fake bloquea un cierre que intente usar URL/DB/ruta/nombre de conector como evidencia; guard de imports mantiene fuera adaptadores de producto; el smoke file temporal mantiene la frontera de `create_job` sin submitter; el smoke HTTP no-OPES arranca app externa temporal con snapshot propio, activa `ORQUESTA_DOMAIN_WORK_HTTP_BASE_URL`, crea job externo real por `/api/v0/domain-work`, verifica replay idempotente, envia artefacto manual con `receipt_ref`, crea run neutral, ejecuta `codex-fake`, entrega artefacto a la app por `submit_artifact` real, registra ACK/delivery, review aceptada, required tests durables y `PlanState` cerrado. | Cerrado para politica productiva como contrato/adaptador y para ruta viva no-OPES con app HTTP/file temporal y submitter real opt-in. Pendiente: Codex/proveedor real opcional y casos amplios de ola/cohorte. |
| DOCPLAN-EXPAND-OFFLINE | Expander neutral `DomainDocumentPlanV0 -> DomainWorkJobRequestV0[]` para jobs derivados sin app externa real. | Offline determinista. | Sin app externa, sin Codex, sin red, sin DB, sin filesystem productivo; el expander vive en capa neutral y acepta solo contratos/refs opacas. | `go test -count=1 ./modulos/orquesta-document-plan-expander`. | Un plan valido produce jobs derivados estables para investigacion, secciones, visuales, tests, revisiones, validacion, ensamblado, audio, tutor/bots e HTML local; cada job conserva `correlation_id`, `idempotency_key`, `requested_by`, `domain_ref`, refs compactas, dependencias y `work_kind` neutral; un plan invalido se rechaza sin jobs parciales; el guard local no permite adaptadores concretos. | Riesgo de esconder juicio de planificacion en un expander deterministico. El expander solo materializa derivados ya declarados por el plan validado; ejecutar jobs, DB y colas pertenece a conectores. |
| WORK-PROFILES-OFFLINE | Perfiles neutrales `WorkProfileV0` y resolucion scheduler sin OPES/Codex. | Offline determinista. | Sin app externa, sin runtime real, sin red, sin DB; solo contratos de core y provider de orquestacion. | `go test -count=1 ./modulos/orquesta-core-workflow ./modulos/orquesta-orchestration-core -run 'TestWorkflowTaskFromWorkProfileV0|TestValidateWorkflowTaskV0RejectsUnknownWorkProfileKind|TestWorkflowTaskCandidateProviderV0.*Profile'`. | `WorkProfileV0` produce `WorkflowTaskV0.work_profile_kind`; perfiles desconocidos se rechazan; el scheduler deriva rol/capacidad por `WorkflowTaskProfileResolverPortV0`; tareas legacy mantienen compatibilidad. | No ejecuta agentes reales ni valida calidad semantica; cada conector debe mapear el perfil neutral a su ejecucion concreta. |
| DOMAIN-WORK-FILE-OFFLINE | Conector durable file-based para `DomainWorkJobRecordStorePortV0`, replay y lectura filtrada de jobs derivados tras reinicio. | Offline determinista. | Sin app externa, sin Codex, sin red, sin DB; usa directorios temporales. | `go test -count=1 ./modulos/orquesta-domain-work/... ./modulos/orquesta-domain-work-memory ./modulos/orquesta-domain-work-file ./cmd/orquesta-server -run 'Test.*DomainWork.*File|TestDomainWorkExecutorFromEnvV0|Test.*DomainWorkJobRecord'`. | Crea jobs aceptados, ejecuta contract tests compartidos sobre memory/file, escribe snapshot JSON versionado, reabre y replaya con el mismo `job_ref`, lista records `Request+Job` con filtros AND y limite, rechaza conflictos sin sobrescribir, falla ante snapshot corrupto, prueba integracion offline con `orquesta-document-plan-expander` y wiring opt-in del servidor para `/api/v0/domain-work create_job`. | No sustituye un conector DB productivo ni un smoke real de app externa. Es adaptador filesystem de referencia, no habilita `submit_artifact` y no debe ser consumido directamente por el nucleo. |
| DOMAIN-WORK-SQL-OFFLINE | Adaptador SQL driver-neutral para `DomainWorkJobRecordStorePortV0`. | Offline determinista con driver fake local. | Sin app externa, sin Codex, sin red, sin driver DB real; usa `database/sql` con driver fake de test. | `go test -count=1 ./modulos/orquesta-domain-work-sql`. | Ejecuta `contracttest` con placeholders `question` y `dollar`, crea jobs aceptados, replaya por idempotencia, rechaza conflictos sin sobrescribir, lista records `Request+Job` con filtros AND y limite, propaga contexto cancelado, simula unique violation concurrente y mantiene frontera sin drivers concretos. | No prueba Postgres/MySQL/SQLite real, no crea schema productivo y no esta cableado al servidor. Un driver real debe entrar despues por composicion opt-in con `IsUniqueViolation` propio. |
| DOMAIN-WORK-SQL-REAL-DIALECT | Bundle futuro con DB temporal real para cada dialecto soportado. | Pendiente documentado; real opt-in por motor. | DB temporal aislada, driver elegido en modulo de composicion, DSN secreto/temporal, migracion/schema explicito, backend no compartido con apps externas. | Sin script claro en repo. No se documenta comando nuevo. | Debe probar schema real, placeholders del driver, unicidad `(domain_ref,idempotency_key)` y `job_ref`, replay tras reinicio, carrera concurrente replay/conflict, tipos JSON/TEXT, filtros/limit y teardown limpio. | No debe importar drivers desde core/domain/director. Riesgo de convertir SQL en persistencia global incompleta si se cablea antes de definir bundle y migraciones. |
| DAEMON-RESTART | Reinicio de daemon con estado file-based: cola persiste tras parar y arrancar servidor. | Smoke offline no invasivo. | `go`, `curl`, `python3`; opcional `ORQUESTA_KEEP_SMOKE_DIR=1`, `ORQUESTA_SMOKE_ROOT`, `ORQUESTA_SMOKE_REQUEST_TIMEOUT_SECONDS`. | `./scripts/smoke_orquesta_server_restart_state.sh`. | Salida con `servidor before listo`, `rank_verificado=<run_ref> priority=80`, `servidor after listo`, verificacion tras reinicio, `priority=95`, `agents_launched=false`, `opes_touched=false`. | No valida agentes reales ni rehidratacion de procesos Codex vivos. Cubre continuidad basica de estado operativo. |
| SERVER-LIVENESS-READINESS | Contrato servidor: `/healthz` es liveness y `/api/v0/server/readiness` es readiness operativa antes de efectos externos. | Offline determinista cerrado 2026-05-26. | Sin Codex real, sin OPES, sin proveedor; scripts solo validan sintaxis. | `go test -count=1 ./modulos/orquesta-server ./modulos/orquesta-cli ./cmd/orquesta-server`; `bash -n scripts/*.sh scripts/lib/*.sh`. | Readiness devuelve 200 solo con `ready=true`, 503 si startup cleanup/reconciliacion queda bloqueado, incluye `startup_ready`, `startup_status`, mensaje publico y evidence refs, y no expone paths ni runtime dirs. El daemon no acepta `/healthz` como listo y los smokes con Codex/OPES/domain_work/Director/automejora esperan readiness. Los helpers compartidos de smokes esperan `/api/v0/server/readiness` antes de efectos externos. | No prueba readiness de apps externas distintas de Orquesta; sus `/healthz` siguen siendo contratos propios de cada app temporal. |
| SERVER-SELF-WATCHDOG-OFFLINE | Autovigilancia del servidor por CPU sostenida sin progreso y parada cooperativa propia. | Offline determinista cerrado; revalidado por OrquestaV2 el 2026-06-11. | Sin Codex real, sin OPES, sin proveedor; CPU/progreso por fuente fake o sampler de proceso local; umbrales por configuracion canonica del servidor. | `go test -count=1 ./modulos/orquesta-server`; `go test -count=1 ./cmd/orquesta-server`. Revalidacion de paquete: `git diff --check`; `go test -count=1 ./cmd/orquesta-server ./modulos/orquesta-runtime-codex ./modulos/orquesta-app-codex-stack ./modulos/orquesta-director-operativo ./modulos/orquesta-rails ./modulos/orquesta-autoprogramming ./modulos/orquesta-mcp`; `go test -count=1 ./cmd/orquesta-server`; `go test -count=1 ./modulos/orquesta-app-codex-stack`. | CPU alta con progreso reciente, outbox, agentes, procesos registrados, ticks activos, Director residente activo, bridge externo activo, async work de apagado o shutdown en curso no pide parada. El tick de self-watchdog cruza tambien el flag atomico del Director residente antes de decidir. CPU alta sostenida sin causa ni progreso marca `self_watchdog_shutdown_requested`, readiness no lista y salida publica `unhealthy`; el runtime apaga el propio servidor con causa `self_watchdog`. `cmd/orquesta-server` publica en `effective_config` `ORQUESTA_SERVER_SELF_WATCHDOG_DISABLED`, `ORQUESTA_SERVER_SELF_WATCHDOG_CPU_HIGH_PERCENT`, `ORQUESTA_SERVER_SELF_WATCHDOG_SUSTAINED_SECONDS` y `ORQUESTA_SERVER_SELF_WATCHDOG_NO_PROGRESS_SECONDS`. | No usa `pkill`, no mata procesos ajenos y no drena OPES/TCAE. No es rail de contenido ni criterio para rechazar entregas. El codigo central vive en `modulos/orquesta-server`; cambios ahi requieren write-set propio. |
| SHUTDOWN-COOP-OFFLINE | Shutdown cooperativo por contrato sin Codex real. | Offline determinista. | Sin procesos externos; paquetes de stack y shutdown. | `go test ./modulos/orquesta-app-codex-stack ./modulos/orquesta-server-shutdown -run 'Test.*Shutdown.*|TestShutdownServerV0.*' -count=1`. | `forced=false` no pide stop si falta checkpoint; con ACK registra checkpoint; deadline vencido fuerza stop con evidencia; `shutdown_ready` solo si no quedan agentes en vuelo ni checkpoints pendientes. | No valida que Codex real lea `orquesta_shutdown_request.json`. |
| SHUTDOWN-COOP-REAL | Shutdown cooperativo Codex real con request y ACK de checkpoint. | Real, opt-in, con cuota. | `ORQUESTA_CODEX_STACK_SHUTDOWN_SMOKE=1`; `ORQUESTA_CODEX_COMMAND`; `ORQUESTA_CODEX_HOME`; `ORQUESTA_CODEX_CODE_HOME`; `ORQUESTA_CODEX_PATH`; `ORQUESTA_CODEX_APPROVAL_POLICY=never`; `ORQUESTA_CODEX_SANDBOX=workspace-write`; `ORQUESTA_CODEX_MODEL`; workdirs temporales dentro del proyecto. | `go test ./modulos/orquesta-app-codex-stack -run TestCodexStackRealShutdownCheckpointOptInV0 -count=1 -timeout 300s -v`. | Agente vivo; `POST /api/v0/server/shutdown forced=false`; se escribe `orquesta_shutdown_request.json`; agente responde `agent_shutdown_checkpoint_ack.json`; segunda llamada registra checkpoint o muestra pending compacto. | Solo cubre Codex cooperativo por prompt; otros runtimes/proveedores siguen pendientes. |
| INVALID-REPLAN-OFFLINE | Entrega invalida, review gate y replan/rework sin procesos reales. | Offline determinista. | Sin Codex real. | `go test -count=1 ./modulos/orquesta-app-codex-stack -run 'TestCodexStackV0ReviewGatePideCambiosSiFicheroEsDemasiadoGrande|TestCodexStackV0ReviewChangesRequestedReplanificaYArrancaAgente|TestValidateDomainWorkDeliveryQualityV0RechazaDocumentPlanIncompleto|TestCompositeDirectorDecisionSourceV0RechazaVoteRefIncoherente'`. | Entrega grande queda `changes_requested`; se emite `RequestRework`; `retry_task` enlaza descriptor de entrega; `document_plan` incompleto se rechaza; lote de director con refs causales rotas no se consume. | No mide calidad semantica del rework real. |
| INVALID-REPLAN-REAL | Review/rework real: app multiagente, evidencia invalida, rework con Codex real y aceptacion final. | Real, opt-in, largo, con cuota. | `ORQUESTA_CODEX_STACK_REVIEW_REWORK_SMOKE=1`; `ORQUESTA_CODEX_COMMAND`; `ORQUESTA_CODEX_HOME`; `ORQUESTA_CODEX_CODE_HOME`; `ORQUESTA_CODEX_PATH`; `ORQUESTA_CODEX_APPROVAL_POLICY=never`; `ORQUESTA_CODEX_SANDBOX=workspace-write`; `ORQUESTA_CODEX_MODEL`; `ORQUESTA_CODEX_SMOKE_TIMEOUT_SECONDS`; workdirs temporales. | `go test ./modulos/orquesta-app-codex-stack -run TestNuevaAppWebCodexStackRealReviewReworkOptInV0 -count=1 -timeout 1100s -v`. | Varios agentes reales de programacion; entrega registrada; evidencia se invalida; review gate pide cambios; Orquesta replanifica `retry_task`; agente real de rework entrega ACK; rework queda `accepted`. | Largo y sensible a cuota/modelo. `tasks=[]` puede ser estado intermedio si `LastSequence` avanza; no subir timeouts antes de diagnosticar la transicion que falta. |
| CODEX-RECURSION-REAL | Recursion Codex gobernada: agente padre, hijos y nietos con parent/child refs, fanout, depth, presupuesto, review causal, runner y cierre por `PlanState`. | Real opt-in cerrado el 2026-05-23; fake-runtime end-to-end, supervisor fake recursivo, split_task generado offline y materializacion CLI fuerte tambien cerrados. | Fake: `go` y workdir temporal. Real: proveedor Codex con cuota, doble confirmacion, `ORQUESTA_CODEX_COMMAND`, `ORQUESTA_CODEX_HOME`, `ORQUESTA_CODEX_CODE_HOME`, OPES env vacio. Guardas: fanout 2, depth 2, 7 agentes, `WaitAgentRefs` por parent/wave y `high` por defecto. | `./scripts/smoke_codex_real_recursive_tree.sh`; materializacion CLI `./scripts/smoke_codex_director_recursive_wave.sh`; focales fake/generadas en `modulos/orquesta-app-codex-stack` y `cmd/orquesta-server`. | El smoke real reutiliza el arbol 1->2->4, espera ACK/entregas Codex reales por nivel antes de abrir hijos/nietos, valida waits acotados por parent/wave, review causal, `RequiredTestRunner`, evidencias por task y cierre del arbol. Resultado observado: PASS en 341.05s con directorio conservado `/tmp/orquesta-codex-real-recursive-tree.JjMGJJ`. | No toca OPES ni colas amplias. Si falla en futuro, el bloqueo queda localizado en arranque por nivel, ACK/entrega, wait acotado por subarbol, review causal, runner/evidencia o cierre. |
| WAIT-COHORT-OFFLINE | Derivacion de espera por cohorte/ola/parent task hacia `WaitAgentRefs` y registro/recuperacion de `WorkflowTaskWaitStateV0`. | Offline determinista. | Sin proveedor real; run y task store en memoria. | `go test -count=1 ./modulos/orquesta-app-director-service ./modulos/orquesta-orchestration-core ./modulos/orquesta-state-file ./modulos/orquesta-app-codex-stack ./cmd/orquesta-server -run 'Test.*WaitAgentRefs|Test.*WorkflowTaskWait|TestExistingDirectorLoopRequestV0|TestStoreV0RecuperaWorkflowTaskWaitState|TestContinueAppDirectorV0RecuperaWaitStatePersistidoSinAmpliarCohorte|TestUpdateOperationalDirectorPlanStateAfterLoopV0AvanzaAReviewTrasWaitConsumido|TestWaitStateStoreV0UsaStoreExplicitoOTaskStoreLegible'`. | `WorkflowTaskWaitAgentRefsV0` carga solo tasks abiertas autorizadas por `run.Tasks`, descarta cerradas y deduplica; `BuildWorkflowTaskWaitSnapshotV0` conserva tasks/agentes pendientes; `app-director-service` resuelve `wait_cohort_ref`, `wait_wave_ref` o `wait_parent_task_ref` antes del loop progresivo; el state-file persiste el wait state; en reentrada con `wait_ref` y `WaitStateStore`, `ContinueAppDirectorV0` prefiere el wait state `waiting` validado para no ampliar la cohorte; al consumir el wait lo marca `continued` sin duplicar evidencia en replay. | No prueba espera real con Codex. Cubre contrato y persistencia offline del filtro previo al loop; el cierre de ingesta Codex esta cubierto aparte por `WAIT-SCOPE-INGEST-OFFLINE`. |
| WAIT-SCOPE-INGEST-OFFLINE | Drain Codex acotado por `WaitAgentRefs`: pending, wait e ingesta de ACK/deliveries usan el mismo scope. | Offline determinista cerrado para P1 WaitAgentRefs. | Sin proveedor real; fake runtime/fuente de observaciones con dos agentes del mismo run, uno dentro de `WaitAgentRefs` y otro fuera. | `go test -count=1 ./modulos/orquesta-orchestration-core ./modulos/orquesta-runtime-codex-delivery ./modulos/orquesta-app-codex-stack -run 'TestDeliveryCandidateProviderV0PropagaYFiltraWaitAgentRefs|TestRunProgressiveLoopV0PropagaWaitAgentRefsAObservationSource|TestCodexDeliveryObservationSourceV0WaitAgentRefs|TestDrainRunV0WaitAgentRefsNoIngiereACKFueraDeScope|TestDrainRunHasPendingExternalAgentsV0FiltraCohorteObjetivo|TestDomainWork.*WaitAgentRefs'`. | `WaitAgentRefs` viaja desde loop progresivo hasta `AgentDeliveryObservationRequestV0`; el source Codex no lee/verifica ACKs fuera del scope; `DrainRunV0` registra solo ACK/delivery dentro del scope; `domain_work` no hace submit/recovery fuera de scope; legacy de run completo sigue cuando `WaitAgentRefs` esta vacio. | Cierra scope de ingesta/pending/wait, no el ciclo funcional completo de una ola. Siguen pendientes smokes reales de replan generico de blockers; OPES temporal real de derivados/cierre quedo cerrado aparte en `OPES-DER-RESTO`. |
| DIRECTOR-GENERIC-CLOSURE-OFFLINE | Cierre causal generico del Director Operativo tras una espera por ola/cohorte. | Offline determinista cerrado para cierre causal; un smoke Codex real pequeno cierra runner/review/cierre y `EXT-NO-OPES` cierra app externa temporal con runtime fake. | Sin proveedor real en tests offline, sin OPES real, sin DB real; core + `app-director-service` + source real del stack Codex sobre task store y eventos persistidos. | `go test -count=1 ./modulos/orquesta-orchestration-core ./modulos/orquesta-app-director-service ./modulos/orquesta-app-codex-stack -run 'TestOperationalDirectorClosureV0|TestContinueAppDirectorV0(CierraCicloReviewRunnerYReplanOrClose|StateFileReplayReviewRunnerReplanOrCloseCloseNoDuplica|InvocaCierreOperativoTrasLoopQuiescent|NoInvocaCierreOperativoMientrasEsperaAgente)|TestMaybeCloseOperationalDirectorV0PasaScopeResueltoAlSource|TestMaybeCloseOperationalDirectorV0PasaRequiredTestEvidenceRefsDelPlanStateAlSource|TestMaybeCloseOperationalDirectorV0(CierraDomainWorkConAppExternaFake|NoCierraDomainWorkConArtefactoNoCausal|DomainWorkReplayNoDuplicaCierre|CierraOlaMultitareaPorReentradasSinBloquear|CierraPlanStateTrasCierreOperativoExitoso|BloqueaPlanStateConIssuesDeCierre|BloqueaPlanStateSiSourceNoConstruyeRequest|BloqueaPlanStateSinClosureSourceEnReplanOrClose|BloqueaPlanStateSinTaskStoreEnReplanOrClose|BloqueaPlanStateConOutboxPendienteEnReplanOrClose|NoCierraConPlanStateFueraDeReplanOrCloseRunning|ReplayNoDuplicaCierreNiPlanState|ReplayNoDuplicaBloqueoDeCierre)|TestContinueRequestWithOperationalDirectorPlanStateV0(ReabreClosureIssuesConFollowupTardio|ReintentaCierreTrasSourceDisponible|ReintentaCierreTrasTaskStoreDisponible|ReintentaCierreTrasOutboxDrenado|StateFileRestartReintentaPrereqSinDuplicar)|TestOperationalClosureSourceV0'`. | El cierre solo se intenta en `quiescent` con outbox cero y, si hay `PlanState` activo, solo desde `replan_or_close` en `running`; con outbox pendiente bloquea `operational-closure-outbox-pending` sin invocar source; si source, task store u outbox faltan temporalmente, la reentrada reabre `replan_or_close` cuando el prerequisito aparece o se drena, incluso tras reiniciar `state-file`, y no duplica blocker/evidencia/eventos al cerrar; el source recibe `WaitAgentRefs` resuelto y `RequiredTestEvidenceRefs` aceptadas por `PlanState`; la peticion se deriva de `WorkflowTaskStore` + eventos; task/delivery/review aceptada deben pertenecer a la misma cadena causal; `programming` exige evidencia durable de tests requeridos; `domain_work` puede cerrar con artefacto validado por app externa fake y refs opacas sin inventar tests de programacion, no cierra si el artefacto no es causal y su replay no duplica `TaskClosed`, validacion ni `RunClosed`; `ContinueAppDirectorV0` cubre en un unico ciclo offline review aceptada, runner de tests por puerto, `replan_or_close` y cierre, y el replay con `state-file` recuperado no duplica `TaskClosed`, validacion, `RunClosed` ni evidencia; si una task cierra pero quedan `run.open_tasks`, no bloquea el `PlanState` y permite reentrar para cerrar las restantes; el cierre exitoso marca el `PlanState` como `closed`; issues reales, source insuficiente, source ausente, task store ausente u outbox pendiente lo bloquean con `closure_reason`; si el cierre falla por `required_test_evidence_refs`, el replan causal reflejado puede reabrir `wait_subagents` solo para el followup tardio; replay de cierre/bloqueo no duplica refs ni `RunClosed`. | `CODEX-REQTEST-REAL-E2E` cubre un agente Codex vivo pequeno hasta cierre. `EXT-NO-OPES` cubre app HTTP/file temporal no-OPES con submitter real opt-in, `codex-fake`, review, required-tests y plan cerrado. OPES temporal real de derivados/cierre quedo cerrado aparte en `OPES-DER-RESTO`; sigue pendiente replan posterior con agentes reales fuera de los caminos ya cubiertos. |
| DIRECTOR-WAVE-REVIEW-OFFLINE | `review_deliveries` por ola/cohorte del Director Operativo. | Offline determinista cerrado para review positiva, observacion negativa durable y reentrada tardia a followups ya materializados. | Sin proveedor real; usa run, eventos durables y `OperationalDirectorPlanStateV0` con `wave_ref`/`cohort_ref`/`parent_task_ref`. | `go test -count=1 ./modulos/orquesta-app-director-service -run 'Test(UpdateOperationalDirectorPlanStateAfterLoopV0ReviewNegativaRegistraReworkReplan|UpdateOperationalDirectorPlanStateAfterLoopV0ReviewChangesRequested(IdempotenteYNoReentraWaitAntiguo|AbreWaitDeSplitFollowups|AbreWaitDeRetryAgent)|UpdateOperationalDirectorPlanStateAfterLoopV0AvanzaDeReviewATestsRequeridos|UpdateOperationalDirectorPlanStateAfterLoopV0AvanzaDeReviewAReplanSinTests|UpdateOperationalDirectorPlanStateAfterLoopV0NoAvanzaReviewFueraDeScope|UpdateOperationalDirectorPlanStateAfterLoopV0NoAvanzaReviewConOutboxPendiente|ContinueRequestWithOperationalDirectorPlanStateV0(ReentraReviewConScope|NoReentraReviewChangesRequested|ReabreReviewChangesRequestedConFollowupTardio)|ContinueAppDirectorV0StateFileReplayReviewChangesRequestedReworkReplanNoDuplica)$'`. | Una ola solo avanza por cadena aceptada `DeliveryRegistered -> ReviewRequested -> ReviewResultRecorded(accepted) -> ReviewAccepted`; entregas de otra ola no avanzan la ola activa. Si el historial durable trae `ReviewResultRecorded(changes_requested|rejected) -> ReworkRequested -> ReplanDecisionRecorded`, el `PlanState` marca `changes_requested`, guarda refs de delivery/review/rework/replan e incrementa attempts; si los followups aparecen despues, `ContinueAppDirectorV0` reabre `wait_subagents` solo con esos refs y no duplica attempts. El replay con `state-file` recuperado conserva scope de review original y no duplica eventos ni proyecciones de rework/replan. | No genera rework/replan nuevo por si mismo; observa eventos ya emitidos por el nucleo. El cierre por quietud sigue bloqueado mientras el plan state este en review/tests. |
| DIRECTOR-REQUIRED-TESTS-DURABLE-OFFLINE | `run_required_tests` durable antes de cierre en modo `programming`. | Offline determinista cerrado como consumo de evidencia, bloqueo por evidencia faltante, runner opt-in integrado, replay focal sin reejecucion externa y replan negativo acotado; smoke Codex real pequeno cerrado. | Sin proveedor real, sin OPES; usa `RequiredTestEvidenceV0` en store in-memory/file y eventos durables del scope. La prueba de servidor ejecuta `go test ./...` sobre modulo temporal aislado. | `go test -count=1 ./modulos/orquesta-orchestration-core ./modulos/orquesta-runtime-required-test ./modulos/orquesta-app-director-service ./modulos/orquesta-app-codex-stack ./cmd/orquesta-server -run 'TestRequiredTestRunnerV0(EsIdempotenteConMismaEvidencia|ConStoreReaderNoReejecutaExternoEnReplay)|TestLocalCommandExecutorV0(NoHeredaEntornoPadre|GoTestSinTestsEsFailed)|TestUpdateOperationalDirectorPlanStateAfterLoopV0(AvanzaDeTestsAReplanConEvidenciaPassed|EjecutaRunnerDeTestsRequeridos|BloqueaTestsSinEvidenciaNiRunnerYReentra|RequiredTestsEvidenceMissingEmiteQualityGateIdempotente|BloqueaTestsConEvidenciaFailed|TestsFailedEmiteQualityGateYReplanAutomatico|NoAvanzaTestsConEvidenciaDeOtraReview)|TestRequiredTestsEvidenceMissingStateFileRestartNoDuplicaGateYReentraConEvidencePassed|TestContinueAppDirectorV0(CierraCicloReviewRunnerYReplanOrClose|StateFileReplayReviewRunnerReplanOrCloseCloseNoDuplica)|TestOperationalClosureSourceV0UsaRequiredTestEvidenceRefsDelRequest|TestBuildStackFromEnvV0RequiredTestRunnerEjecutaGoTestYPersisteEvidencia'`. | `review_deliveries` activa tests en modo `programming`; `run_required_tests` carga refs explicitas de `RequiredTestEvidenceRefs` o, por compatibilidad del stack, refs de evidencia del review result; cuando falta evidencia y hay runner inyectado, genera `RequiredTestEvidenceV0` durable por puerto; si no hay evidencia causal ni runner efectivo, bloquea con `required-tests-evidence-missing`, emite un `QualityGateRecorded(blocked)` idempotente sin `ReplanDecisionRecorded`, no duplica el gate tras reiniciar `state-file`, y reentra si la evidencia `passed` aparece despues; si la evidencia deterministica ya existe y el writer tambien es reader o se inyecta reader, el runner no reejecuta el comando externo; la composicion de servidor opt-in ejecuta un `go test ./...` real, persiste evidencia en `state-file`, avanza `PlanState` y cierra; el executor local no hereda variables del proceso padre cuando `Env` esta vacio y marca `failed` un `go test` que no ejecuta ningun paquete con tests reales; solo acepta `passed` con run/task/test/delivery/review request/review result/accepted review exactos; `failed` de un unico task genera `QualityGateRecorded(blocked)` + `ReplanDecisionRecorded(retry_task)` y deja capacidad/agente al scheduler. | No toca OPES. El tramo fake-runtime/runner esta cerrado; la ejecucion con Codex real amplio queda cubierta por `CODEX-WAVE-REAL` y `CODEX-RECURSION-REAL`. |
| CODEX-REQTEST-STATEFILE-SMOKE | Cableado opt-in de composicion Codex real configurada + `RequiredTestRunner` + `state-file`, sin lanzar Codex real. | Smoke opt-in no invasivo; ejecuta solo `go test ./...` en proyecto Go temporal. | `go`, `python3`, `curl`; `ORQUESTA_CODEX_REQUIRED_TEST_SMOKE_CONFIRM=1`; `ORQUESTA_CODEX_COMMAND` debe resolver a un ejecutable Codex real, aunque el smoke solo valida configuracion y no lo ejecuta; opcional `ORQUESTA_KEEP_SMOKE_DIR=1`, `ORQUESTA_SMOKE_ROOT`, `ORQUESTA_REQUIRED_TEST_ENV`, `ORQUESTA_CODEX_REQUIRED_TEST_SMOKE_TIMEOUT`. | `./scripts/smoke_codex_required_test_runner_state_file.sh`. | Crea workdir, runtime, HOME Codex y state dir temporales; fuerza OPES desactivado; cablea stack Codex + `RequiredTestRunner`; siembra run/eventos/task/`OperationalDirectorPlanStateV0` en `state-file`; `ContinueAppDirectorV0` ejecuta `go test ./...` sobre modulo temporal, persiste `RequiredTestEvidenceV0`, recupera desde nuevo stack file-based, cierra run/plan state, valida JSON del state-file, arranca servidor temporal y lo apaga por `/api/v0/server/shutdown`, y confirma `codex_executed=false`, `opes_touched=false`. | No prueba entrega de un agente Codex vivo ni review real generada por Codex; el ejecutable Codex se valida como configuracion opt-in pero no se invoca. |
| CODEX-REQTEST-REAL-E2E | Agente Codex real pequeno + entrega registrada + review causal aceptada + `RequiredTestRunner` + cierre de plan/run. | Real opt-in, acotado, con cuota; cerrado el 2026-05-22 y revalidado el 2026-06-11; no CI por defecto. | `go`; workdir/runtime/HOME Codex explicitos en modo real; `ORQUESTA_CODEX_REAL_REQUIRED_TEST_RUNNER_CONFIRM=1`; `ORQUESTA_CODEX_REAL_REQUIRED_TEST_RUNNER_EXECUTE_CODEX=1`; `ORQUESTA_CODEX_REAL_REQUIRED_TEST_RUNNER_CODEX_EXECUTION_CONFIRMED=1`; `ORQUESTA_CODEX_COMMAND`; `ORQUESTA_CODEX_HOME`; `ORQUESTA_CODEX_CODE_HOME`; `ORQUESTA_CODEX_APPROVAL_POLICY=never`; `ORQUESTA_CODEX_SANDBOX=workspace-write`; `ORQUESTA_CODEX_REASONING_EFFORT=medium`; OPES env vacio. | `./scripts/smoke_codex_real_required_test_runner.sh`. Prueba directa: `go test -count=1 ./modulos/orquesta-app-codex-stack -run '^TestCodexStackRealRequiredTestRunnerEndToEndOptInV0$' -timeout 720s -v`. | El modo E2E siembra una task del Director Operativo con `RequiredTests=go test ./...`, arranca un unico agente Codex real por `WaitAgentRefs`, drena ACK/entrega del agente, guarda `OperationalDirectorPlanStateV0` en `review_deliveries/running`, abre revision, drena la review con `operational_director_plan_ref`, espera `ReviewRequested -> ReviewResultRecorded(accepted) -> ReviewAccepted`, propaga `accepted_review_refs` a `run_required_tests`/`replan_or_close`, ejecuta `RequiredTestRunner`, persiste `RequiredTestEvidenceV0` y exige `run.status=closed` y plan state `closed`. | Verificado el 2026-05-22: `./scripts/smoke_codex_real_required_test_runner.sh` con Codex real paso en 64.49s. Revalidado el 2026-06-11 tras corregir propagacion de `accepted_review_refs` y harness con `operational_director_plan_ref`: PASS en 256.29s. Si falla en futuro, el bloqueo verificable queda en el punto exacto: arranque Codex, ACK/entrega, review aceptada, runner/evidencia o cierre causal. No toca OPES ni core. |
| CODEX-GOAL-FIRST-APP-SERVER-REAL | `/nueva-app` goal-first con Codex app-server: Orquesta compila `GoalWorkSpecV0`, Codex Goal ejecuta el loop interno y Orquesta cierra por marcador estructurado o resultado durable. | Real opt-in, acotado, con cuota; cerrado localmente el 2026-06-26 con `app_server_tmux` y repetido en contenedor aislado el 2026-06-29. | `go`, `curl`, `python3`, `codex app-server`; backend operativo `ORQUESTA_CODEX_GOAL_BACKEND=app_server_tmux`; `app_server_proxy` no es backend operativo ni diagnostico suficiente para cerrar el smoke vigente aunque aparezca en notas historicas; doble confirmacion `ORQUESTA_CODEX_GOAL_FIRST_APP_SERVER_REAL_CONFIRM=1` y `ORQUESTA_CODEX_GOAL_FIRST_APP_SERVER_CODEX_EXECUTION_CONFIRMED=1`; OPES env vacio; opcional `ORQUESTA_KEEP_SMOKE_DIR=1`, `ORQUESTA_CODEX_MODEL`, `ORQUESTA_CODEX_REASONING_EFFORT`, `ORQUESTA_CODEX_GOAL_TIMEOUT_MS`; en contenedor remoto aislado puede requerir `ORQUESTA_CODEX_SANDBOX=danger-full-access` con `ORQUESTA_CODEX_APPROVAL_POLICY=never` porque `workspace-write` no siempre admite el proyecto temporal bajo `/workspace/runtime`. | `./scripts/smoke_goal_first_app_server_real.sh`; runbook `docs/runbooks/smoke_goal_first_app_server_real_2026-06-25.md`. Focales sin proveedor: `go test -count=1 ./modulos/orquesta-runtime-codex-goal ./cmd/orquesta-server`. | El script crea proyecto temporal con contexto minimo, valida el backend app-server seleccionado, levanta `orquesta-server` temporal con `ORQUESTA_CODEX_GOAL_BACKEND`, llama `POST /api/v0/apps/director`, observa por `POST /api/v0/apps/director/goal/observe`, lee `thread/goal/get` y `thread/read`, extrae `ORQUESTA_GOAL_RESULT_V0` o `orquesta_goal_result_v0.json`, fusiona `artifact_refs`/`evidence_refs` y exige `director_execution_mode=goal_first`, `goal_status=complete`, `closure_status=accepted`, `closure_accepted=true` y run `cerrada`. Evidencia 2026-06-26: `artifact_refs=2`, `evidence_refs=9`, `smoke_root=/tmp/orquesta-goal-first-app-server.2dZTDh`. Evidencia 2026-06-29: `run_ref=run-spec-smoke-goal-first-req-smoke-goal-first-617c6ba0738874be15af06aee294d3e9`, `external_goal_ref=019f131c-b884-7410-8f36-2344d0c37da3`, `artifact_refs=2`, `evidence_refs=9`, `smoke_root=/workspace/runtime/smokes/orquesta-goal-first-app-server.CD5sKB`. | No toca OPES ni colas amplias. No sustituye `CODEX-WAVE-REAL`/`CODEX-RECURSION-REAL`: valida el nuevo reparto goal-first, donde Codex Goal hace el loop interno y Orquesta valida cierre externo. |
| CODEX-WAVE-REAL | Ola/cohorte Codex amplia: varios agentes de una misma `wave_ref`/`cohort_ref`, espera acotada por `WaitAgentRefs`, review causal por entrega, `RequiredTestRunner` y cierre de plan/run. | Fake-runtime verde por defecto; real opt-in cerrado con proveedor en el corte `ccedb14b`. | Fake: sin env, sin Codex real. Real via script con doble confirmacion, `ORQUESTA_CODEX_COMMAND`, `ORQUESTA_CODEX_HOME`, `ORQUESTA_CODEX_CODE_HOME`, approval `never`, sandbox amplio o workspace y `high`; OPES env vacio. | `./scripts/smoke_codex_real_operational_wave.sh`. Pruebas directas: fake `go test -count=1 ./modulos/orquesta-app-codex-stack -run '^TestCodexStackOperationalWaveFakeRuntimeV0$'`; real `go test -count=1 ./modulos/orquesta-app-codex-stack -run '^TestCodexStackRealOperationalWaveOptInV0$' -timeout 900s -v` con env opt-in. | El harness siembra 3 `WorkflowTaskV0` en la misma ola/cohorte, arranca agentes por refs derivadas de cohorte/ola, drena ACK/deliveries solo con `WaitAgentRefs`, abre revision, exige cadena causal aceptada por entrega, construye `OperationalDirectorPlanStateV0` multitarea, ejecuta `go test ./...` por `RequiredTestRunner`, persiste evidencias por task y cierra run/plan. | No cubre recursion parent/child ni OPES temporal real. Si falla en futuro, el bloqueo queda localizado en arranque paralelo Codex, ACK/entrega, review aceptada, runner/evidencia causal por task o cierre multitarea. |
| AUTOPROGRAMMING-SUPERVISED-FAKE | Autoprogramacion desatendida sin Codex real: request valida, trabajo programable, entrada real Go del stack, ruta REST/MCP `prepare-run`, enqueue en cola global, supervisor residente sin `run_ref`, idempotencia de replay, fallback legacy external-work solo con opt-in, `director_execution_mode=legacy_director_loop` y cierre offline por tests focales disponibles. | Smoke opt-in no invasivo; fake/temporal. | `go`, `python3`, `curl`; `ORQUESTA_AUTOPROGRAMMING_SUPERVISED_SMOKE_CONFIRM=1`; `ORQUESTA_AUTOPROGRAMMING_LEGACY_DIRECTOR_LOOP=1`; opcional `ORQUESTA_KEEP_SMOKE_DIR=1`, `ORQUESTA_SMOKE_ROOT`, `ORQUESTA_SMOKE_REQUEST_TIMEOUT_SECONDS`, `ORQUESTA_SERVER_TICK_INTERVAL_MS`, `ORQUESTA_SMOKE_RESIDENT_POLLS`, `SMOKE_ID`; compatibilidad historica `ORQUESTA_AUTOPROGRAMMING_LEGACY_EXTERNAL_FALLBACK=1` + `ORQUESTA_EXTERNAL_WORK_LEGACY_DIRECTOR_LOOP=1`. | `./scripts/smoke_autoprogramming_supervised.sh`; runbook `docs/runbooks/smoke_autoprogramming_supervisado.md`. | `go test` de `orquesta-autoprogramming`; harness temporal ejecuta `BuildAutoprogrammingProgrammableWorkV0` y emite `programmable_work_ok`; tests focales cubren `PrepareAutoprogrammingRunV0`, `CodexStackAutoprogrammingExecutorV0` y `POST /api/v0/autoprogramming/prepare-run`, validando run continuable, `WorkflowTaskV0`, `WaitAgentRefs`, request `continue`, candidato en cola global y supervisor global con stack fake; servidor temporal valida `/api/v0/autoprogramming/validate-request`, ejecuta `/api/v0/autoprogramming/prepare-run` con opt-in legacy y `director_execution_mode=legacy_director_loop`, espera a que el supervisor residente tome la cola global y arranque Codex fake para el `run_ref` preparado, y repite `prepare-run` con el mismo payload para exigir `run_ref` estable, `wait_agent_refs` no vacio y `continue`; no llama por defecto a `/api/v0/external-work/run` ni a `/api/v0/runs/supervise`; esos pasos y sus focales legacy solo corren con los opt-in legacy correspondientes y payload legacy explicito; ejecuta pruebas focales existentes de stack fake, supervisor y cierre offline; salida confirma `resident_supervisor_agents_started`, `prepare_run_replay_ok=true`, `codex_real_executed=false` y `opes_touched=false`. | No sustituye smoke Codex real ni recursion gobernada. La validacion de replay requiere que el patch idempotente y el enqueue del stack esten disponibles; sin esos patches, este caso debe fallar antes de tocar Codex real u OPES. `external-work/run` no es launcher de autoprogramacion; queda para trabajos externos neutrales y este smoke lo trata como compatibilidad legacy explicita. Si el bridge de workers no-Codex o rutas de compatibilidad no estan cableados, el smoke documenta el prerequisito como `skip` sin fallar. |
| AUTOPROGRAMMING-BOLSA-REAL | App externa Go real por autoprogramacion: Orquesta crea arquitectura hexagonal y tests para `Bolsa_Diputacion_app` usando Codex real, dependencias por refs fuente remapeadas, supervisor residente, review, required-tests y cierre. | Real opt-in, con cuota, cerrado el 2026-06-19. | App temporal/local `~/Trabajo/Bolsa_Diputacion_app`; servidor Orquesta temporal con state/runtime aislados; `ORQUESTA_CODEX_PROJECT_WORKDIR` a la app; `ORQUESTA_CODEX_RUNTIME_WORKDIR` temporal; `ORQUESTA_CODEX_MODEL=gpt-5.5`; `ORQUESTA_CODEX_REASONING_EFFORT=high`; `ORQUESTA_CODEX_APPROVAL_POLICY=never`; `ORQUESTA_CODEX_SANDBOX=workspace-write`; `ORQUESTA_REQUIRED_TEST_RUNNER_ENABLED=1`; `ORQUESTA_AUTOPROGRAMMING_LEGACY_DIRECTOR_LOOP=1`; OPES env vacio. | `POST /api/v0/autoprogramming/prepare-run` con `~/Trabajo/Bolsa_Diputacion_app/orquesta_spec_nucleo.json` y payload `director_execution_mode=legacy_director_loop`; segundo run focal de tests `bolsa-tests-001`; consultar `/api/v0/director/stats`; verificacion externa `go test -count=1 ./...` en la app. | `bolsa-nucleo-001` cerro 8/8 tareas con Codex real, 8 entregas, 8 reviews aceptadas, 1 validacion y 1 closure; `bolsa-tests-001` cerro 4/4 tareas de tests sin `runs/supervise` manual, relanzando `g02` y `g04` solo cuando sus dependencias llegaron a `DeliveredTasks`/`ClosedTasks`; la app queda con tests reales para dominio, auth, i18n, repositorio, casos de uso y handler HTTP; `go test -count=1 ./...` devuelve `ok` en paquetes con tests reales y solo `ports` como interfaz sin tests. | No toca OPES ni declara cerrados smokes OPES. Convertir esta evidencia en script/runbook opt-in repetible queda como mejora operativa; futuros fallos deben abrir incidencia nueva con stats/run refs, no reabrir P1 WaitAgentRefs salvo regresion demostrada. |
| AUTOPROGRAMMING-PROMOTION-E2E | Promocion de staging de automejora residente: run cerrada causalmente, review aceptada, evidencia `RequiredTestEvidenceV0` passed, commit local acotado y archivo idempotente. | E2E local con repo Git temporal; cerrado el 2026-05-24 para promocion local sin push. | `git` y `go`; sin proveedor real, sin OPES, sin HOME ni remoto. El stack usa puerto opt-in y el adaptador `orquesta-runtime-worktree` sobre repo temporal. | `go test -count=1 ./modulos/orquesta-autoprogramming ./modulos/orquesta-runtime-worktree ./modulos/orquesta-app-codex-stack ./cmd/orquesta-server`; runbook `docs/runbooks/autoprogramacion_staging_promocion_2026-05-24.md`. | `TestCodexStackAutoprogrammingPromotionV0E2ERepoTemporalReplayV0` siembra una run de autoprogramacion cerrada, task store, `PlanState` cerrado y evidencia passed; el stack llama al puerto de promocion, `GitStagingPromotionConnectorV0` crea un commit solo para `feature.md`, el archivo de staging conserva refs opacas y el replay del tick no duplica commits ni manifests. `TestCodexStackAutoprogrammingPromotionV0NoCierraColaConEfectoIncompletoV0` verifica que `pending_push` o archivo bloqueado no producen `queue_status=closed`. | No habilita push remoto/productivo. Remoto, rama, ventana operativa y confirmacion de operador quedan en T39/app-vcs guards. |
| DIRECTOR-REPLAN-CLOSE-OFFLINE | `replan_or_close`/`close` causal del Director Operativo. | Cerrado para los caminos offline/fake-runtime probados: close, puerta `replan_or_close`, replay de cierre, ola multitarea por reentradas, bloqueo/retry de source/task store/outbox, evidencia faltante, tests fallidos, cierre insuficiente causal y replan causal multitarea. | Sin proveedor real; deliveries, reviews, tests, outbox y task store fakes; usa `OperationalClosureSource` opcional para evaluar cierre offline. | `go test -count=1 ./modulos/orquesta-app-director-service ./modulos/orquesta-orchestration-core -run 'TestMaybeCloseOperationalDirectorV0(CierraOlaMultitareaPorReentradasSinBloquear|CierraPlanStateTrasCierreOperativoExitoso|BloqueaPlanStateConIssuesDeCierre|BloqueaPlanStateSiSourceNoConstruyeRequest|BloqueaPlanStateSinClosureSourceEnReplanOrClose|BloqueaPlanStateSinTaskStoreEnReplanOrClose|BloqueaPlanStateConOutboxPendienteEnReplanOrClose|ReplanCausalSiCierreInsuficienteConReviewAceptada|ReplanCausalSiFaltaValidationRefConReviewAceptada|NoCierraConPlanStateFueraDeReplanOrCloseRunning|ReplayNoDuplicaCierreNiPlanState|ReplayNoDuplicaBloqueoDeCierre)|TestContinueRequestWithOperationalDirectorPlanStateV0(ReabreClosureIssuesConFollowupTardio|ReintentaCierreTrasSourceDisponible|ReintentaCierreTrasTaskStoreDisponible|ReintentaCierreTrasOutboxDrenado)|TestOperationalDirectorClosureV0|TestOperationalDirectorPlanEmitClosureIssueReplanDecisionV0NoDuplicaSiRunYaReflejaDecision|TestQualityGateReplanCandidateProviderV0|TestOperationalDirectorPlanStateAfterRequiredTestsReplanV0ReabreSoloFollowupCausalEnScopeMultitarea|TestUpdateOperationalDirectorPlanStateAfterLoopV0BloqueaTestsSinEvidenciaNiRunnerYReentra'`. | Cierra tarea/ola/run solo con review aceptada, evidencias, tests requeridos, estado `quiescent`, outbox cero y `PlanState` en `replan_or_close/running`; si hay outbox pendiente, bloquea `operational-closure-outbox-pending` sin llamar source y reintenta al drenar; si una task cierra y aun quedan abiertas, `run.open_tasks` se trata como progreso parcial reentrable y no como bloqueo; si falta evidencia de test causal, bloquea con `required-tests-evidence-missing` y reentra cuando aparece evidencia valida; si hay cierre insuficiente causal con `closure_ref`/`validation_ref`, emite `QualityGateRecorded(blocked)` + `ReplanDecisionRecorded(retry_task)` y reabre solo el followup reflejado; si esos refs ya estan reflejados en el run, no reemite comandos aunque falten `CommandEffects` frescos; si el cerrador devuelve issues reales sin cadena reparable, el source no construye request, falta source o falta task store, bloquea el `PlanState` con causa y reintenta cuando el prerequisito vuelve a existir; quality gate bloqueante + replan reflejado genera candidates para que el scheduler pida capacidad/agente; en scopes multitarea, un replan causal reflejado para una task fallida reabre espera solo para su followup; si un cierre ya bloqueado por `required_test_evidence_refs` recibe despues el agente de followup reflejado, reabre `wait_subagents` sin volver al agente antiguo; replay de cierre/bloqueo no duplica refs ni `RunClosed`. | Pendiente real: replan posterior con agentes Codex reales fuera de los caminos ya cubiertos; OPES temporal real de derivados/cierre quedo cerrado aparte en `OPES-DER-RESTO`. |
| DIRECTOR-PLAN-STATE-OFFLINE | `OperationalDirectorPlanStateV0`: estado vivo del plan operativo. | Offline determinista cerrado para el ciclo probado: plan inicial en start/continue, reentrada, wait->review, wait expirado/limpiado, review positiva y negativa, runner por puerto, tests durables, evidencia faltante, test fallido, replan causal multitarea, cierre/bloqueo post-closure y recuperacion durable de state-file. | Sin proveedor real; store durable/fake para plan state, run, `WorkflowTaskStore`, `WorkflowTaskWaitStateV0`, eventos/outbox y evidencias fakes. | `go test -count=1 ./modulos/orquesta-orchestration-core ./modulos/orquesta-state-file ./modulos/orquesta-app-director-service ./modulos/orquesta-app-codex-stack ./cmd/orquesta-server -run 'Test.*OperationalDirectorPlanState|Test.*PlanState|TestStartAppDirectorV0OperationalDirectorPlan|TestContinueAppDirectorV0.*PlanState|TestContinueAppDirectorV0CierraCicloReviewRunnerYReplanOrClose|TestContinueRequestWithOperationalDirectorPlanStateV0|TestContinueAppDirectorV0RecuperaWaitStatePersistidoSinAmpliarCohorte|TestContinueAppDirectorV0BloqueaWaitSubagentsCuandoManagedWaitExpira|TestMaybeCloseOperationalDirectorV0MarcaWaitStateClearedAlCerrarPlanState|TestUpdateOperationalDirectorPlanStateAfterLoopV0|TestMaybeCloseOperationalDirectorV0|TestOperationalPlanStateStoreV0|TestWaitStateStoreV0|TestStoreV0RecuperaEstadoTrasRecrearInstancia|TestBuildStackFromEnvV0UsaConectoresDurablesFileBased|TestBuildStackV0RequiresOptInAndExplicitPorts|TestBuildStackFromEnvV0RequiredTestRunnerEjecutaGoTestYPersisteEvidencia'`. | DTO, validacion, store en memoria y store file-based existen; `StartAppDirectorV0` y `ContinueAppDirectorV0` pueden sembrar `launch_subagents -> wait_subagents` con plan listo y contratos funcionales explicitos, reentra por `operational_director_plan_ref`, recupera `WorkflowTaskWaitStateV0` persistido cuando el step activo trae `wait_ref`, marca el wait state `continued` al avanzar a `review_deliveries`, marca `expired` y bloquea `PlanState`/step con `external-wait-exhausted` solo cuando el loop gestionado transporta agotamiento real de `MaxExternalWaits`; tras reiniciar `state-file`, el wait expirado no reabre espera ni duplica evidencias; marca `cleared` los waits antiguos aun `waiting` cuando el cierre operativo exitoso clausura el plan, registra refs causales de review positiva, ejecuta runner de tests requerido por puerto cuando falta evidencia, consume `RequiredTestEvidenceV0`, bloquea `required-tests-evidence-missing` cuando no hay evidencia causal y reentra si aparece despues, genera replan retry para test fallido de un task, consume replan causal parcial en scopes multitarea, registra review negativa con refs de `ReworkRequested`/`ReplanDecisionRecorded`, reabre waits para followups causales que llegan tarde sin esperar agentes globales, marca cierre/bloqueo con `closure_reason` y recupera tras recrear `state-file` tasks, wait state y plan state con ola/cohorte/parent refs. | OPES temporal real de derivados/cierre quedo cerrado aparte en `OPES-DER-RESTO`. |
| DIRECTOR-TARDE-OFFLINE | Corte minimo para dejar el Director continuable hoy: contrato operativo, materializador parcial, start/continue con plan inicial, waits por metadata, ingesta acotada, plan state, review/tests/cierre offline y replan probado. | Offline determinista. | Sin proveedor real. Usar stores en memoria/fakes de los paquetes existentes. | `go test -count=1 ./modulos/orquesta-director-operativo ./modulos/orquesta-orchestration-core ./modulos/orquesta-app-director-service ./modulos/orquesta-app-codex-stack -run 'Test.*OperationalDirector|Test.*WaitAgentRefs|Test.*ContinueAppDirector|Test.*StartAppDirector.*OperationalDirector|Test.*ReviewRework|Test.*Replan|TestDrainRunV0WaitAgentRefsNoIngiereACKFueraDeScope'`. | Plan operativo valido, materializacion de `launch_subagents`, refs de ola/cohorte preservadas, `StartAppDirectorV0` con `OperationalDirectorPlanV0` inicial solo si hay contratos funcionales explicitos, waits derivados por metadata, `WorkflowTaskWaitStateV0` persistible, plan state persistible, reentrada con refs concretas, `DrainRunV0` no ingiere ACK fuera de scope, review positiva del PlanState avanza por cadena causal, tests durables por `RequiredTestEvidenceV0` pasan o bloquean, runner por puerto genera evidencia cuando esta inyectado, `replan_or_close` gobierna el cierre, review/rework/replan offline sigue pasando y replay `state-file` no duplica cierre ni replan por tests fallidos. | No prueba runtime real ni recursion con hijos/nietos. El materializador sigue limitado a `launch_subagents` por diseno; OPES temporal real de derivados/cierre quedo cerrado aparte en `OPES-DER-RESTO` y los huecos reales restantes son blockers nuevos fuera de `CODEX-WAVE-REAL` y `CODEX-RECURSION-REAL`. |
| DIRECTOR-CYCLE-SOURCE-OFFLINE | Fuente de verdad del ciclo Director V2: `DirectorCycleStepV0 -> director-runner -> director-scheduler -> core-workflow -> director-cycle-outbox`. | Offline determinista documental/focal. | Sin proveedor real. Usar ledger, scheduler y workflow por puertos/fakes locales. | `go test -count=1 ./modulos/orquesta-director-cycle ./modulos/orquesta-director-scheduler ./modulos/orquesta-director-runner ./modulos/orquesta-director-tick-input ./modulos/orquesta-orchestration-core`. | El tick neutral consulta outbox pendiente, construye input compacto, planifica comandos publicos, aplica workflow por puerto, registra outbox nueva y se detiene ante outbox/bloqueo/error. La documentacion raiz y local distingue esta espina del loop historico `app-director-service`/loop progresivo y clasifica pendientes como codigo offline, composicion residente, smoke real, proveedor real u OPES temporal. | No declara cerrado un smoke real ni la composicion residente. `T65` cubre restart/dispatch/ACK del servidor; `T66` cubre proceso temporal real neutral. |
| NEUTRAL-PROCESS-STOP-E2E | Stop neutral de proceso real `DIR-P006`: Director/scheduler/outbox hasta `StopRuntimeAgent` contra `ProcessRuntimeConnectorV0`. | E2E local con proceso temporal real; cerrado el 2026-05-25. | Sin Codex, sin OPES, sin proveedor externo. Usa binario de test como proceso temporal, env minimo sin HOME heredado, `state-file`/`FileOutboxLedgerV0` por stack temporal. | `go test -count=1 ./modulos/orquesta-runtime ./modulos/orquesta-director ./modulos/orquesta-director-scheduler ./modulos/orquesta-director-cycle ./modulos/orquesta-orchestration-core ./cmd/orquesta-server`. Focal: `go test -count=1 ./cmd/orquesta-server -run TestNeutralProcessStopE2EV0 -v`. | `TestNeutralProcessStopE2EV0` lanza proceso temporal por `ProcessRuntimeConnectorV0`, registra `run_ref`/`agent_ref`/`process_ref` en el registry, genera supervision `loop_detected`, ejecuta `DirectorCycleStepV0`, persiste `StopRuntimeAgent` en outbox durable, despacha con `AgentStopperExecutorV0` + `ProcessAgentStopperV0`, registra `AgentStopConfirmed`, ACKea el outbox y reconstruye stack con mismo state dir para validar replay `no_pending`. Revisa el estado persistido para no filtrar command path, env, `working_dir`, HOME, tokens ni secretos. | No cubre escalado avanzado de kill timeout/stopping, politica completa de launch/env/IO ni shutdown checkpoint T30. T60 sigue siendo el rail Codex por registry/PID. |
| USAGE-METRICS-OFFLINE | Metricas de uso por agente y resumen por run sin proveedor real. | Offline determinista. | Sin proveedor real; fake usage source. | `go test -count=1 ./modulos/orquesta-orchestration-core ./modulos/orquesta-mcp ./modulos/orquesta-web ./modulos/orquesta-app-codex-stack -run 'TestBuildDirectorRunStatsWithTelemetryPortsV0IncluyeUsoDeAgente|TestMCPDirectorStatsToolExecutorV0IncluyeUsoDeAgentesOptIn|TestRESTConsultarDirectorStatsClientV0EnviaPOSTJSONYProyectaPanel|TestCodexStackAgentUsageSourceV0UneMetricasInyectadas|TestCodexStackV0DirectorStatsIncluyeProcesoYProgresoPorPuertos'`. | Stats incluyen `Usage`, `UsageSummary`, modelo/capacidad, tokens y cuota; web pide `include_agent_usage`; MCP solo incluye uso en opt-in. | Conector productivo de cuota/tokens por proveedor sigue pendiente; coste real depende de politica futura. |
| USAGE-METRICS-REAL | Stats REST de servidor con proceso, progreso y uso durante run real. | Smoke real, puede lanzar Codex. | `go`, `curl`, `python3`; `codex` y env de runtime si se quiere agente real; opcional `ORQUESTA_SMOKE_STATS_POLLS`, `ORQUESTA_SMOKE_MIN_STARTED_AGENTS`, `ORQUESTA_SMOKE_MIN_PARALLEL_AGENTS` como alias compatible, `ORQUESTA_KEEP_SMOKE_DIR`. | `./scripts/smoke_orquesta_server_rest_director.sh`. | `POST /api/v0/apps/director -> HTTP 2xx`; `run_ref`; tres polls a `/api/v0/director/stats`; salida con `stats_verificadas=process_refs,progress,usage,director_start`; parada limpia. | Valida el arranque REST directo `start-only` del director, no una ola paralela del supervisor/drain. El script no tiene guarda de confirmacion propia; tratarlo como real y no meter en CI rapida. |

## Secuencia recomendada

1. Ejecutar primero los casos offline: `DAEMON-RESTART`,
   `SHUTDOWN-COOP-OFFLINE`, `INVALID-REPLAN-OFFLINE`,
   `USAGE-METRICS-OFFLINE`, `EXT-NO-OPES`, `WAIT-COHORT-OFFLINE`,
   `WAIT-SCOPE-INGEST-OFFLINE` y despues
   `DIRECTOR-GENERIC-CLOSURE-OFFLINE`.
2. Ejecutar smokes OPES sin agente contra OPES temporal:
   `OPES-DOMAIN-REST` y `OPES-DER-VISUAL-REST`.
3. `OPES-PLAN-TEMARIO-OPERADORES` ya quedo cerrado para plan real acotado.
   Ejecutar smokes OPES con agente real cuando haya cuota y OPES aislado:
   `OPES-DER-DRAFT-AGENT`, derivados de ese plan y despues `OPES-PLAN-REAL`.
4. Repetir smokes Codex largos solo en ventana operativa controlada o ante
   regresion: `CODEX-WAVE-REAL`, `SHUTDOWN-COOP-REAL`,
   `INVALID-REPLAN-REAL` y `USAGE-METRICS-REAL`.
   `CODEX-REQTEST-STATEFILE-SMOKE` queda como smoke no invasivo de
   configuracion/runner con `state-file`, y `CODEX-REQTEST-REAL-E2E` ya cerro
   el caso acotado con agente Codex vivo. Para validar el nuevo reparto
   goal-first sin reabrir el Director Operativo historico, ejecutar
   `CODEX-GOAL-FIRST-APP-SERVER-REAL` solo con backend Codex app-server, cuota
   y proyecto temporal confirmados.

## Bateria final propuesta

Esta bateria no sustituye `go test -count=1 ./...` para un corte transversal
final. Sirve para validar que la foto documental y los casos foco no quedan
stale antes de pedir smokes reales:

```bash
git diff --check -- \
  docs/matriz_pruebas_reales_y_smoke_2026-05-17.md \
  docs/estado_actual_2026-05-17.md \
  docs/corte_cierre_generico_director_operativo_2026-05-17.md \
  modulos/orquesta-app-codex-stack/docs/pruebas.md

go test -count=1 \
  ./modulos/orquesta-director-operativo \
  ./modulos/orquesta-orchestration-core \
  ./modulos/orquesta-app-director-service \
  ./modulos/orquesta-app-codex-stack \
  -run 'Test.*OperationalDirector|Test.*WaitAgentRefs|Test.*ContinueAppDirector|Test.*ReviewRework|Test.*Replan|TestDrainRunV0WaitAgentRefsNoIngiereACKFueraDeScope'

go test -count=1 \
  ./modulos/orquesta-orchestration-core \
  ./modulos/orquesta-runtime-required-test \
  ./modulos/orquesta-state-file \
  ./modulos/orquesta-app-director-service \
  ./modulos/orquesta-app-codex-stack \
  ./cmd/orquesta-server \
  -run 'TestRequiredTestRunnerV0|Test.*OperationalDirectorPlanState|Test.*PlanState|TestUpdateOperationalDirectorPlanStateAfterLoopV0|TestMaybeCloseOperationalDirectorV0|TestContinueAppDirectorV0(CierraCicloReviewRunnerYReplanOrClose|StateFileReplayReviewRunnerReplanOrCloseCloseNoDuplica)|TestBuildStackFromEnvV0RequiredTestRunnerEjecutaGoTestYPersisteEvidencia'

./scripts/smoke_external_domain_fake_real.sh
./scripts/smoke_external_domain_non_opes_real.sh
./scripts/smoke_codex_required_test_runner_state_file.sh
```

Los smokes reales con Codex u OPES siguen opt-in y fuera de CI rapida. Para
regresion del caso acotado ya cerrado, usar
  `./scripts/smoke_codex_real_required_test_runner.sh` solo con confirmaciones,
cuota y workdirs temporales validados. Ola/cohorte Codex amplia y recursion real
ya estan cubiertas por `CODEX-WAVE-REAL` y `CODEX-RECURSION-REAL`; no son
backlog abierto salvo regresion demostrada. Derivados OPES reales quedaron
cerrados funcionalmente por goal-first temporal en `OPES-DER-RESTO`; quedan
residuales de calidad editorial, coste y automatizacion larga. La ruta no-OPES
temporal con `codex-fake` esta
cubierta por `EXT-NO-OPES`; repetirla con proveedor Codex real es smoke opcional
de proveedor, no prerequisito del cierre no-OPES.

## Siguiente incremento

El siguiente paquete de pruebas debe seguir siendo offline y determinista antes
de cualquier smoke Codex/OPES real. Las casillas siguientes son pendientes hasta
que haya test claro y se actualice la fila correspondiente de la matriz.

- [x] `DIRECTOR-WAVE-REVIEW-OFFLINE`: queda cerrada la salida positiva por
  review aceptada, entrega ajena al scope y observacion negativa con
  `ReworkRequested`/`ReplanDecisionRecorded` en el `PlanState`.
- [x] `DIRECTOR-RECURSIVE-SPLIT-OFFLINE`: `review/rework/replan split_task`
  rechaza followups recursivos con `delegation_depth` imposible o fanout sobre
  `max_child_agents`, y acepta el caso valido con `parent_task_ref`,
  `wave_ref` y `cohort_ref` conservados desde `WorkflowTaskStore`. No sustituye
  smoke Codex real ni presupuesto proveedor.
- [x] `DIRECTOR-REQUIRED-TESTS-DURABLE-OFFLINE`: `run_required_tests` consume
  `RequiredTestEvidenceV0` causal, avanza con `passed`, bloquea con `failed`,
  bloquea `required-tests-evidence-missing` si no hay evidencia causal ni runner
  efectivo y rechaza evidencia de otra review. El runner por puerto ya genera
  evidencias y en replay no reejecuta comandos con evidencia deterministica existente.
  El smoke Codex real acotado con ese runner queda cerrado en `CODEX-REQTEST-REAL-E2E`.
- [x] `DIRECTOR-REPLAN-CLOSE-OFFLINE`: close offline ya marca el `PlanState` como
  cerrado o bloqueado, y `replan_or_close` ya es puerta explicita de cierre con
  replay focal de cierre/bloqueo. El cierre de ola multitarea ya progresa por
  reentradas cuando `run.open_tasks` solo indica que quedan tareas por cerrar.
  Outbox no vacio ya bloquea el `PlanState` sin consultar source. Cierre
  insuficiente causal ya produce `QualityGateRecorded` + `ReplanDecisionRecorded`
  y reabre solo el followup reflejado, incluso en una ola multitarea si el fallo
  identifica una unica task causal del scope. La emision no duplica comandos si
  esos refs deterministas ya estan reflejados en el run. Source, task store y
  outbox pendiente ya reintentan cierre cuando el prerequisito vuelve. Cualquier
  blocker nuevo sin cadena causal debe entrar como caso nuevo con prueba propia,
  no como hueco abierto de este corte.
- [x] `DIRECTOR-PLAN-STATE-OFFLINE`: el corte cubre reentrada, wait->review,
  review positiva, tests durables, review negativa observada con reentrada
  tardia a followups ya materializados, bloqueo por evidencia faltante, puerta
  de `replan_or_close`, recuperacion de wait state persistido en reentrada,
  marcado `continued` al consumir el wait, marcado `expired` ante agotamiento
  real de `MaxExternalWaits`, marcado `cleared` al cerrar el plan y
  cierre/bloqueo. El smoke Codex real acotado con runner esta cerrado en
  `CODEX-REQTEST-REAL-E2E`; `CODEX-WAVE-REAL` y `CODEX-RECURSION-REAL` cierran
  ola/cohorte real amplia y recursion real.
  Debe demostrar que no se reconstruye desde stats ni desde agentes vivos
  globales.
- [x] Evento/comando idempotente: el cierre exitoso, el bloqueo de cierre y el
  replan automatico por tests fallidos tienen replay focal sin duplicar eventos
  ni refs. El runner de tests no reejecuta si la evidencia deterministica ya
  existe, review aceptada no duplica refs del `PlanState`, y el replan usa
  claves estables derivadas de run/task/delivery/review/evidencia, no de
  timestamps ni summaries.

No mover estos casos a "cerrado" por existir `WaitAgentRefs`, por pasar un
smoke real o por tener un source de cierre parcial. Cada frente necesita
evidencia focal propia.

## Huecos pendientes

- Derivados OPES de revision, validacion, ensamblado y paquete final quedaron
  cerrados funcionalmente contra instancia OPES temporal por goal-first en
  `OPES-DER-RESTO`; no reejecutar contra OPES real/productivo para cerrar este
  hueco salvo regresion y confirmaciones opt-in.
- El smoke real no-OPES con servidor temporal ya cubre `create_job` por REST,
  idempotencia, snapshot file-based, `external-work/run`, ejecucion de
  `codex-fake` por runtime/servidor, artefacto, delivery durable sin OPES,
  recuperacion de delivery tardia tras wait expirado, review aceptada,
  reentrada viva post-delivery por `/api/v0/runs/supervise` en ruta legacy,
  runner fake de required tests, evidencia durable y cierre operativo. Esa
  evidencia queda clasificada como compatibilidad historica; falta equivalente
  goal-first no-OPES sin empujar el loop antiguo y falta convertir ese validador
  fake en politica productiva de tests de dominio no-OPES.
- El expander neutral de `DomainDocumentPlanV0 -> DomainWorkJobRequestV0[]`
  tiene cobertura offline y ya se integra con un conector durable file-based;
  falta un smoke real de app externa no acoplada.
- `generate_visual_asset` tiene smoke REST directo, pero falta agente real.
- La recursion Codex real ya esta cubierta por `CODEX-RECURSION-REAL`: arbol
  1->2->4 con agentes vivos, parent/child refs, presupuesto, waits acotados,
  review causal, runner/evidencia y cierre del arbol. No reabrir salvo
  regresion demostrada.
- La ola/cohorte Codex real ya esta cubierta por `CODEX-WAVE-REAL`, con
  `WaitAgentRefs` derivados de scope formal del Director Operativo e ingesta
  acotada. No reabrir salvo regresion demostrada.
- `DIRECTOR-GENERIC-CLOSURE-OFFLINE` tiene tramo offline cerrado para cierre
  causal, source real del stack Codex sobre tasks del Director Operativo,
  puerta `replan_or_close`, bloqueo por cierre insuficiente y replay focal. El
  caso real acotado con runner esta cubierto por `CODEX-REQTEST-REAL-E2E`, y
  Codex real amplio/recursivo por `CODEX-WAVE-REAL` y
  `CODEX-RECURSION-REAL`. OPES temporal real de derivados/cierre quedo cerrado
  en `OPES-DER-RESTO`; los abiertos son residuales editoriales, coste,
  automatizacion larga o regresiones nuevas.
- `DIRECTOR-WAVE-REVIEW-OFFLINE` queda cerrado offline para review positiva,
  observacion negativa durable y reentrada tardia a followups ya materializados.
- `DIRECTOR-REQUIRED-TESTS-DURABLE-OFFLINE` queda cerrado como consumo de
  evidencia durable causal antes de cierre. Incluye runner opt-in, bloqueo por
  evidencia faltante, fallo con quality gate/replan de un task y reentrada si
  aparece evidencia causal despues. `CODEX-REQTEST-REAL-E2E` ya cubre un agente Codex real pequeno con runner y cierre causal.
- `DIRECTOR-REPLAN-CLOSE-OFFLINE` queda cerrado para los caminos
  offline/fake-runtime probados: `replan_or_close` gobierna el cierre con review
  aceptada, evidencias, refs de tests durables si aplica, estado `quiescent` y
  outbox cero; outbox pendiente bloquea con causa durable sin invocar source y
  reintenta al drenarse; source/task store ausentes bloquean y reintentan cuando
  aparecen; cierre insuficiente causal ya replanifica por quality gate, tambien
  en scopes multitarea con una unica task afectada, y no reemite comandos si la
  pareja gate/replan ya esta reflejada en el run. Codex real amplio y recursivo
  ya esta cubierto por `CODEX-WAVE-REAL` y `CODEX-RECURSION-REAL`; OPES
  temporal real de derivados/cierre quedo cerrado aparte en `OPES-DER-RESTO`;
  quedan pendientes nuevos blockers demostrables.
- `DIRECTOR-PLAN-STATE-OFFLINE` tiene contrato, stores, escritura inicial,
  reentrada, avance wait->review, salida positiva review->tests/replan, consumo
  de tests durables, bloqueo por evidencia faltante y cierre/bloqueo post-closure
  cerrados para el ciclo fake-runtime probado. El replay con `state-file` ya
  cubre cierre, bloqueo de cierre y replan por tests fallidos sin duplicar la
  verdad de `WorkflowTaskStore`, `WorkflowTaskWaitStateV0`, eventos/outbox ni
  `RequiredTestEvidenceV0`.
- `CODEX-REQTEST-STATEFILE-SMOKE` cubre runner opt-in y persistencia con
  configuracion Codex validada, pero no ejecuta agente Codex vivo.
  `CODEX-REQTEST-REAL-E2E` ya une entrega Codex, review causal, runner de tests
  y cierre de Director Operativo en caso acotado; `CODEX-WAVE-REAL` y
  `CODEX-RECURSION-REAL` cubren Codex real amplio y recursivo. OPES temporal
  real de derivados/cierre queda cerrado funcionalmente por goal-first en
  `OPES-DER-RESTO`; quedan pendientes residuales de calidad editorial, coste y
  automatizacion larga.
- Shutdown cooperativo esta validado para Codex; otros runtimes/proveedores
  necesitan su propio puerto de checkpoint.
- Uso/coste real necesita conector productivo de proveedor; hoy la cobertura
  offline prueba el contrato y la web/MCP, no el dato remoto.
